import assert from "node:assert/strict";

const verifyTrailerVideoArtifact = async (): Promise<void> => {
  assert.fail("trailer-video artifact verifier has not been implemented");
};

void verifyTrailerVideoArtifact();
