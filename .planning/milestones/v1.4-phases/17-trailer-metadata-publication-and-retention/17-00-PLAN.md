---
phase: 17-trailer-metadata-publication-and-retention
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/scripts/verify-youtube-trailer-publication.ts
  - package.json
autonomous: true
requirements: [TRAILER-08]
must_haves:
  truths:
    - A compiled offline verifier and its temporary SQLite/media fixtures exist before implementation plans run.
    - Existing artifact downloads and Phase 16 draft staging/promotion/rollback are explicit regression gates for Phase 17.
    - The verifier has a fake-provider seam and network/OAuth tripwire ready for later publication assertions.
  artifacts:
    - src/scripts/verify-youtube-trailer-publication.ts
    - package.json
  key_links:
    - The Wave 0 fixture harness is reused by Plans 01-03 instead of creating parallel test databases or media roots.
    - The compiled command runs after build and remains offline.
---

<objective>
Create the Wave 0 verifier harness and fixture contract that all Phase 17 implementation plans depend on.

Purpose: Nyquist validation must begin with executable evidence, including the existing artifact-download and Phase 16 draft safety contracts, before publication code is added.
Output: Temporary SQLite/media fixture helpers, fake-provider/network tripwire seam, baseline regression assertions, and the compiled verifier script command.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-trailer-metadata-publication-and-retention/17-CONTEXT.md
@.planning/phases/17-trailer-metadata-publication-and-retention/17-RESEARCH.md
@.planning/phases/17-trailer-metadata-publication-and-retention/17-VALIDATION.md
@.planning/phases/16-draft-staging-and-private-youtube-job/VERIFICATION.md
@src/scripts/verify-trailer-video-upload-lifecycle.ts
@src/scripts/verify-youtube-trailer-job-lifecycle.ts
@src/scripts/verify-episode-artifact-downloads.ts
@package.json
</context>

<tasks>
<task type="auto">
  <name>Task 1: Register the compiled Phase 17 verifier command</name>
  <files>package.json</files>
  <action>Add `verify:youtube-trailer-publication` using the repository's compiled `dist/scripts` command shape with `NODE_ENV=development` and `DISABLE_BACKGROUND_WORKERS=true`. Keep the existing `verify:episode-artifact-downloads`, `verify:trailer-video-upload-lifecycle`, and `verify:youtube-trailer-job-lifecycle` scripts unchanged. Do not add a dependency or a frontend command. This registration must complete before the harness task invokes the command.</action>
  <verify><automated>node -e "const p=require('./package.json'); if(!p.scripts['verify:youtube-trailer-publication']) process.exit(1)"</automated>
  <done>The compiled verifier command is registered before the Wave 0 harness runs, while all existing verifier commands remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create the offline fixture and fake-provider harness</name>
  <files>src/scripts/verify-youtube-trailer-publication.ts</files>
  <action>Per TRAILER-08 and the Phase 16 verification contract, create a compiled Node assert verifier that creates isolated temporary SQLite and media roots before dynamic application imports and removes them in finally. Add reusable fixture builders for an episode, current trailer, draft reservation, promoted final artifact, rollback failure, legacy version files, and a fake YoutubeTrailerUploadProvider event log. The fake provider must expose a fetch/OAuth/network tripwire that fails any un-injected live request. Add baseline assertions that invoke the existing artifact-download verifier command contract and explicitly prove draft reservation, staging, promotion, failed-create rollback, and last-known-good final preservation. Keep this scaffold runnable before publication implementation exists; later plans extend the same file and fixtures rather than replacing the harness.</action>
  <verify><automated>npm run build && npm run verify:youtube-trailer-publication && npm run verify:episode-artifact-downloads && npm run verify:trailer-video-upload-lifecycle</automated>
  <done>Wave 0 passes entirely offline, creates no files under the real data roots, proves artifact-download plus draft staging/promotion/rollback behavior, and fails immediately on live network/OAuth access.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Verifier -> application code | Temporary fixtures and injected providers must prevent production state or live provider side effects. |
| Verifier -> filesystem | Fixture cleanup must not traverse configured application media roots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-17-00-01 | Tampering | fake provider harness | high | mitigate | Install a network/OAuth tripwire and require every provider call to be injected. |
| T-17-00-02 | Denial of Service | fixture cleanup | medium | mitigate | Use unique temporary roots and finally cleanup; never derive paths from request input. |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install is introduced. |
</threat_model>

<verification>
Wave 0 is green only when the compiled verifier, existing artifact-download verifier, and Phase 16 upload/promotion/rollback verifier all pass offline.
</verification>

<success_criteria>
Plans 01 and 02 can depend on an executable fixture/verifier command that already covers artifact-download and draft staging/promotion/rollback regressions.
</success_criteria>

<output>
Create `.planning/phases/17-trailer-metadata-publication-and-retention/17-00-SUMMARY.md` when done.
</output>
