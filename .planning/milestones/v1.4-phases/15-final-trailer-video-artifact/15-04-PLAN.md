---
phase: 15-final-trailer-video-artifact
plan: 04
type: execute
wave: 3
depends_on: [15-02, 15-03]
files_modified:
  - src/scripts/verify-trailer-video-artifact.ts
  - package.json
  - src/docs/openapi.ts
  - .env.example
  - README.md
autonomous: true
requirements: [TRAILER-01, TRAILER-05]
must_haves:
  truths:
    - "Repository-native verification proves the protected MP4 upload/replacement path, manual-sync transition, and final-video artifact ZIP inclusion."
    - "OpenAPI and operator documentation describe the authenticated upload, only-MP4 rule, 500 MiB default, trailer-video selector, and no-path contract."
  artifacts:
    - path: src/scripts/verify-trailer-video-artifact.ts
      provides: "Compiled offline regression verifier for final trailer-video upload and artifact behavior"
    - path: package.json
      provides: "Dedicated runnable trailer-video verification command"
    - path: src/docs/openapi.ts
      provides: "Protected multipart upload and expanded fixed selector OpenAPI contract"
    - path: .env.example
      provides: "Documented configurable trailer-video byte limit"
  key_links:
    - from: package.json
      to: src/scripts/verify-trailer-video-artifact.ts
      via: "compiled NODE_ENV=development npm verification script"
      pattern: "verify:trailer-video-artifact"
    - from: src/docs/openapi.ts
      to: src/routes/episodes.routes.ts
      via: "same authenticated POST path, multipart file field, and artifact selector set"
      pattern: "trailer-video"
---

<objective>
Close Phase 15 with executable end-to-end contract coverage and accurate operator/API documentation.

Purpose: Ensure the safety-critical upload and artifact behaviors remain testable, discoverable, and aligned with the protected route before Phase 16 adds external YouTube publication.
Output: Compiled verifier command plus OpenAPI and environment documentation.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-final-trailer-video-artifact/15-CONTEXT.md
@.planning/phases/15-final-trailer-video-artifact/15-02-SUMMARY.md
@.planning/phases/15-final-trailer-video-artifact/15-03-SUMMARY.md
@src/scripts/verify-episode-artifact-downloads.ts
@src/docs/openapi.ts
@.env.example
@README.md

<interfaces>
From Phase 15 Plans 02–03:
- `POST /v1/episodes/{episodeId}/trailer-video` is protected, uses multipart field `file`, and hands a staged MP4 to the final replacement service.
- `trailer-video` is a closed artifact selector for final `trailer.mp4`.
- The final video metadata and sync state are returned through the existing protected episode contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an offline executable verifier for upload, replacement, sync, and ZIP wiring</name>
  <files>src/scripts/verify-trailer-video-artifact.ts, package.json</files>
  <behavior>
    - The compiled verifier confirms it is running from `dist` with `NODE_ENV=development`, creates isolated canonical fixtures, and cleans them in `finally`.
    - It exercises the real protected route stack without opening a listener, proving missing auth is rejected and a valid multipart MP4 updates a real fixture episode.
    - It proves a replacement before publication is accepted, a replacement after a recorded publication reference changes to manual-sync-required while retaining that reference, and the workflow makes no YouTube request.
    - It asserts non-MP4 inputs and uploads over the configured maximum are rejected without leaving a final file.
    - It starts the existing protected artifact job for trailer-video and asserts the resulting ZIP has exactly the canonical final video entry.
  </behavior>
  <action>Create a focused compiled verifier using the existing direct Express-router and temporary-fixture conventions, but construct a real in-memory multipart request stream so Multer and the protected route are exercised without a localhost listener. Create/clean an isolated episode row and server-derived media directories in `finally`; use the existing job service to process the trailer-video ZIP. Temporarily control auth bypass and the parsed byte-limit only inside the verifier, restore all globals in `finally`, and assert no server path is present in responses or captured logs. Add `verify:trailer-video-artifact` to package scripts as a compiled development command. The verifier must prove D-01 through D-05 behaviors assigned to this phase and must not perform OAuth, YouTube, or version-pruning actions reserved for Phase 16.</action>
  <verify><automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run verify:episode-artifact-downloads &amp;&amp; npm run verify:trailer-video-artifact</automated></verify>
  <done>A single runnable repository command proves auth, MP4/type/size rejection, canonical replacement, manual-sync transition, safe cleanup, and final-video ZIP selection without external services.</done>
</task>

<task type="auto">
  <name>Task 2: Document the protected upload, selector, and configuration contract</name>
  <files>src/docs/openapi.ts, .env.example, README.md</files>
  <action>Add OpenAPI for authenticated `POST /v1/episodes/{episodeId}/trailer-video`: multipart `file`, MP4-only acceptance, configurable server-enforced size limit, canonical final artifact semantics, protected episode response, and safe 400/401/404 outcomes. Extend the artifact-job selector enums/descriptions to include D-03 `trailer-video` and explain that it archives only canonical `trailer.mp4`. Add `EPISODE_TRAILER_VIDEO_MAX_BYTES=524288000` with its 500 MiB default and validation semantics to `.env.example`; add a concise README authenticated-endpoint and configuration section that distinguishes final video from the existing audio trailer, explains manual publication re-sync after replacement, and states that clients never provide paths.</action>
  <verify><automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run verify:trailer-video-artifact</automated></verify>
  <done>OpenAPI, env template, README, runtime route, and verifier agree on the protected MP4 upload, 500 MiB default, canonical filename, manual-sync meaning, and trailer-video ZIP selector.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API/OpenAPI/env documentation → administrator | Operator configuration and request construction must not widen the media boundary. |
| verifier fixtures → production-like route/service behavior | Offline verification must prove real boundaries without calling external services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-10 | Spoofing | documented upload route | high | mitigate | Document bearer authentication and prove the real route rejects unauthenticated multipart input. |
| T-15-11 | Tampering | documented selector/config contract | medium | mitigate | Keep OpenAPI enums and env documentation aligned with the closed runtime catalog and parsed maximum through compiled assertions. |
| T-15-12 | Information disclosure | verifier diagnostics/docs | medium | mitigate | Assert safe response/log output and document canonical names, never internal filesystem locations. |
| T-15-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation is authorized by this plan. |
</threat_model>

<verification>
Run `npm run typecheck`, `npm run build`, `npm run verify:episode-artifact-downloads`, and `npm run verify:trailer-video-artifact` in that order.
</verification>

<success_criteria>
- The executable verifier exercises the real protected upload boundary and ZIP lifecycle without an external service.
- The docs and runtime contract describe MP4-only, canonical `trailer.mp4`, 500 MiB default, manual re-sync, and fixed `trailer-video` selection consistently.
</success_criteria>

<output>
Create `.planning/phases/15-final-trailer-video-artifact/15-04-SUMMARY.md` when done.
</output>
