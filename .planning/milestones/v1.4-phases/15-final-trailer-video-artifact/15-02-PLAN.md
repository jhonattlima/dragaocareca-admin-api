---
phase: 15-final-trailer-video-artifact
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/config/env.ts
  - src/services/episode-trailer-video.service.ts
  - src/routes/episodes.routes.ts
autonomous: true
requirements: [TRAILER-01]
must_haves:
  truths:
    - "An authenticated administrator can upload or replace a single final MP4 trailer video for an existing episode."
    - "The route stores the current video only at the canonical server-selected final path and returns the updated protected episode record."
    - "A replacement is permitted before or after publication and changes the sync status to manual action required without contacting YouTube."
    - "Oversize video uploads are rejected using a configurable maximum that defaults to 500 MiB."
  artifacts:
    - path: src/services/episode-trailer-video.service.ts
      provides: "Atomic final trailer-video replacement and sync-state transition"
    - path: src/routes/episodes.routes.ts
      provides: "Authenticated multipart POST endpoint for final trailer-video upload"
    - path: src/config/env.ts
      provides: "Validated trailer-video upload limit"
  key_links:
    - from: src/routes/episodes.routes.ts
      to: src/services/episode-trailer-video.service.ts
      via: "the authenticated Multer boundary gives a server-side staged file to the replacement service"
      pattern: "trailer-video"
    - from: src/services/episode-trailer-video.service.ts
      to: src/services/episode-media-layout.service.ts
      via: "the service derives staging/final paths from the episode id and trailer-video kind"
      pattern: "getEpisodeMedia.*Path"
---

<objective>
Expose the protected MP4 upload/replacement workflow for the canonical final trailer video.

Purpose: Give administrators an immediately usable final artifact while preserving a reliable signal for later manual YouTube synchronization per D-01, D-04, and D-05.
Output: Validated configuration, replacement service, and authenticated multipart route.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-final-trailer-video-artifact/15-CONTEXT.md
@.planning/phases/15-final-trailer-video-artifact/15-01-SUMMARY.md
@src/config/env.ts
@src/routes/episodes.routes.ts
@src/services/episode-media-layout.service.ts

<interfaces>
From Phase 15 Plan 01:
- The trailer-video media kind resolves to `episodes/{episodeId}/trailer.mp4` and has a distinct protected episode metadata field.
- `EpisodeRow` contains the publication-sync state that Phase 16 will consume.

From `src/routes/episodes.routes.ts`:
- `buildUploader(spec)` uses Multer disk storage, per-spec byte limits, extension and MIME checks, and a singular multipart field named `file`.
- Routes apply `requireAuth` before their upload handler.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse and validate the trailer-video upload maximum</name>
  <files>src/config/env.ts</files>
  <behavior>
    - Unset `EPISODE_TRAILER_VIDEO_MAX_BYTES` resolves to 524288000 bytes.
    - A positive integer environment value is used as the upload boundary.
    - Non-integer, zero, and negative values fail startup/config parsing with a descriptive error.
  </behavior>
  <action>Add a narrowly scoped environment parser and expose `config.media.trailerVideoMaxBytes`. It must read `EPISODE_TRAILER_VIDEO_MAX_BYTES`, default to 500 * 1024 * 1024 bytes per D-05, and reject invalid values during configuration loading. Do not reuse the smaller existing audio-trailer limit or accept a client-provided limit.</action>
  <verify><automated>npm run typecheck &amp;&amp; npm run build</automated></verify>
  <done>The final trailer-video Multer spec has one validated server-side maximum, defaulting to 500 MiB when no environment override exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement protected atomic final trailer-video replacement</name>
  <files>src/services/episode-trailer-video.service.ts, src/routes/episodes.routes.ts</files>
  <behavior>
    - POST `/v1/episodes/:episodeId/trailer-video` requires admin auth and one multipart `file`.
    - Only an MP4 filename extension together with an allowed MP4 MIME type can reach the replacement service; other files and over-limit uploads fail without becoming final artifacts.
    - A valid file for an existing episode replaces the canonical final file, persists its server-derived relative filename, and returns the updated episode with its manual-sync state.
    - A replacement after a prior publication reference keeps that reference but marks the video for manual re-sync; no upload path invokes YouTube.
  </behavior>
  <action>Create a focused service that receives an existing episode id and a staged server-side file, confirms the episode exists, atomically promotes it to the D-02 canonical final path, then persists the server-derived filename and D-04 sync transition. Preserve failure safety: remove a rejected or failed staged upload, do not expose staging/final paths in JSON or logs, and do not leave a partial final replacement. Add a dedicated route using the existing Multer factory pattern with path suffix `trailer-video`, multipart field `file`, D-01 MP4 extension/MIME allowlists, and `config.media.trailerVideoMaxBytes`; it must require auth before the upload handler. Keep this distinct from the `trailer` audio route and permit replacement regardless of current publication state.</action>
  <verify><automated>npm run typecheck &amp;&amp; npm run build</automated></verify>
  <done>The authenticated video route accepts only valid, in-limit MP4 uploads for real episodes, makes the artifact immediately available at the canonical final path, reports updated protected metadata, and records later manual sync without calling YouTube.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| administrator multipart request → Multer | Untrusted filename, declared MIME type, file bytes, and size enter the server. |
| staged file → final media storage | A temporary uploaded file becomes a canonical downloadable artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-03 | Spoofing | trailer-video upload route | high | mitigate | Put `requireAuth` before the multipart handler and verify the compiled route stack rejects missing credentials. |
| T-15-04 | Tampering | multipart filename/type/path | high | mitigate | Enforce D-01 extension and MIME allowlists, D-05 Multer size limit, and derive all staging/final names from server-owned episode helpers. |
| T-15-05 | Denial of service | upload body | medium | mitigate | Apply the validated finite Multer file-size limit before promotion and delete staged failures. |
| T-15-06 | Information disclosure | route errors/logs | medium | mitigate | Return safe validation errors and exclude staging/final filesystem paths from responses and structured logs. |
| T-15-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation is authorized by this plan. |
</threat_model>

<verification>
Compile after wiring the config, service, and route. Plan 15-04 adds the full offline multipart/auth/artifact regression verifier.
</verification>

<success_criteria>
- The route is protected and accepts only MP4 input.
- The only final destination is `episodes/{id}/trailer.mp4`.
- Replacement works regardless of publication history and marks the protected sync state for a manual update.
- The default maximum is 500 MiB and an environment override is validated.
</success_criteria>

<output>
Create `.planning/phases/15-final-trailer-video-artifact/15-02-SUMMARY.md` when done.
</output>
