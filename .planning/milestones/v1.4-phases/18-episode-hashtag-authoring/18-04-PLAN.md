---
phase: 18-episode-hashtag-authoring
plan: "04"
type: execute
wave: 4
depends_on:
  - 18-03
files_modified:
  - src/scripts/verify-episode-hashtag-authoring.ts
  - package.json
autonomous: true
requirements:
  - TRAILER-06
must_haves:
  truths:
    - "Compiled offline verification proves the complete automatic 50-candidate, strict 90/10 cache-miss reservation, durable serialized retry/restart recovery, stale summary guard, ranking, and protected manual-route contract."
    - "Verification proves provider/OAuth/network data never reaches a browser DTO or test execution."
  artifacts:
    - path: src/scripts/verify-episode-hashtag-authoring.ts
      provides: "Fake-provider lifecycle, cache/quota, stale-write, route, and OpenAPI verifier"
    - path: package.json
      provides: "Build-output verifier command"
  key_links:
    - from: package.json
      to: dist/scripts/verify-episode-hashtag-authoring.js
      via: "build-first offline verification command"
---

<objective>
Complete and run the repository-native proof suite for the Phase 18 authoring and manual lookup lifecycle.

Purpose: Validate the high-risk quota, stale-state, provider-redaction, and protected-route boundaries without any live Gemini or YouTube request.
Output: A complete fake-provider verifier and final build/typecheck command coverage.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-episode-hashtag-authoring/18-CONTEXT.md
@.planning/phases/18-episode-hashtag-authoring/18-RESEARCH.md
@.planning/phases/18-episode-hashtag-authoring/18-03-PLAN.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-VALIDATION.md
@src/scripts/verify-episode-hashtag-authoring.ts
@src/scripts/verify-youtube-trailer-job-lifecycle.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove automatic authoring, durable retry recovery, quota, and stale-write behavior offline</name>
  <files>src/scripts/verify-episode-hashtag-authoring.ts</files>
  <behavior>
    - A fake Gemini response with 50 valid unique records produces 50 serial lookup attempts, and only relevant records can occupy the count-ranked top three.
    - Fresh cache entries prevent provider calls; manual and automatic requests share normalized identity/display case but stay within strict non-borrowable 90/10 Pacific-day reservations.
    - Fake malformed Gemini, provider/auth/transient/quota failures persist unavailable/retry state while summary remains done; a fake-clock restart recovers pending/interrupted/due work once through the serialized lane and newer summary state rejects every old write.
  </behavior>
  <action>Expand the foundation, lookup, and lifecycle verifier branches into deterministic integration coverage using temporary SQLite/media roots plus injected fake Gemini, OAuth, clock, timer scheduler, and search adapters. Assert schema-constrained 50-record handling, local rejection of malformed/duplicate/unsupported sets, canonical lower-case display casing, one serial lookup lane, all-50 lookup coverage, cache hit/miss/zero expiry behavior, and strict Pacific-day 90 automatic / 10 manual admission without cross-class borrowing. With the fake clock, prove 1/5/15/60-minute retry scheduling, startup recovery of pending and interrupted processing state, recovery of due unavailable state after a simulated process restart, and a stop callback that clears timer work; assert the same episode never executes concurrently or duplicates a provider call. Exercise summary completion through exported production service seams so a tag failure demonstrably leaves `aiSummary` done and creates no second state file. Force summary regeneration between authoring start and terminal/retry writes, then prove root state version plus SHA-256 summary digest blocks stale results and timer callbacks. Include a live-boundary tripwire that fails the verifier if fetch, token refresh, or configured production persistence is reached.</action>
  <verify>
    <automated>npm run build && npm run verify:episode-hashtag-authoring -- --focus=lifecycle</automated>
  </verify>
  <done>The compiled verifier proves automatic authoring, strict daily reservation, durable restart recovery, and stale-result rejection entirely with fake dependencies and disposable storage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove protected manual route, DTO redaction, and OpenAPI parity</name>
  <files>src/scripts/verify-episode-hashtag-authoring.ts, package.json</files>
  <behavior>
    - Direct protected-router invocation applies no-store before authentication and accepts only one valid manual tag body.
    - Cache hit/miss/manual-denial/unavailable responses use safe normalized DTOs and omit token, provider payload, prompt, transcript, summary, absolute-path, and cache-ledger data.
    - OpenAPI exposes exactly the protected manual endpoint and persisted snapshot fields that the route returns.
  </behavior>
  <action>Extend the route-focused branch to invoke the real Express router stack with in-memory authenticated and unauthenticated requests, without opening a listener. Assert no-store on success and failure, Zod body rejection, canonical lower-case manual display casing, shared cache/admission usage, approximate retrieval metadata, and safe unavailable/retry results. Parse the emitted OpenAPI document and assert endpoint, bearer security, request grammar, response schemas, approximate-count naming, canonical display-case documentation, and absence of provider-sensitive/internal fields. Make the final npm verifier command build first and execute the complete foundation, lookup, lifecycle, and route suite with background workers disabled except for the explicitly injected lifecycle worker; retain the four focused commands for fast feedback.</action>
  <verify>
    <automated>npm run typecheck && npm run build && npm run verify:episode-hashtag-authoring</automated>
  </verify>
  <done>The final offline suite proves the protected client contract, documentation parity, safe redaction, and zero live-provider behavior for TRAILER-06.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Generate/expose advisory tags after completed transcript+summary with independent YouTube relevance lookup | 01-04 | COVERED | State, provider, route, and executable proof form a reachable flow.
REQ | TRAILER-06 | Gemini structured 50 candidates; normalized cached/rate-limited counts; relevant top three; protected manual count | 01-04 | COVERED | Plan 02 delivers all-50 service behavior; Plan 03 exposes it; Plan 04 proves it offline.
RESEARCH | — | Same sequential summary tail; local schema validation; stale state/digest guard | 01-04 | COVERED | Plans 01/02/03 define, implement, and verify the guard.
RESEARCH | — | Durable serialized retry execution and startup recovery | 01,03-04 | COVERED | Plan 03 adds persisted-state recovery worker; Plan 04 proves fake-clock restart recovery.
RESEARCH | — | SQLite cache, one lane, strict Pacific 90/10 daily reservation, bounded retries | 01-04 | COVERED | Plans 01/02 establish non-borrowable allocations; Plan 04 proves admission and retry behavior.
RESEARCH | — | Canonical lower-case display-case policy | 01-04 | COVERED | State/normalizer/OpenAPI and verifier share one public display rule.
RESEARCH | — | Approximate count semantics, URLSearchParams, existing OAuth, safe DTO/no raw data | 02-04 | COVERED | Search boundary and route/OpenAPI verifier cover all elements.
CONTEXT | D-01 | Summary then Gemini 50 candidates grounded only in transcript/saved summary | 02-03 | COVERED | Plan 02 prompt/validator and Plan 03 durable sequencing cite D-01.
CONTEXT | D-02 | Look up all 50 normalized candidates and return three | 02-04 | COVERED | Plan 02 executes all lookups; Plan 04 asserts count.
CONTEXT | D-03 | Select high-count only from relevant candidates | 02-04 | COVERED | Plan 02 ranking and Plan 04 tie/ranking coverage.
CONTEXT | D-04 | Failure never fails transcript/summary; unavailable/retry persistence | 01-04 | COVERED | State contract, isolated queueing, and fake failure coverage.
CONTEXT | D-05 | One `suggestedTags` child in existing summary state | 01,03,04 | COVERED | Schema, API read, and no-second-file proof.
CONTEXT | D-06 | Protected single-tag normalized approximate-count lookup | 02-04 | COVERED | Shared search boundary, route, and direct router test.
CONTEXT | D-07 | API owns cache/rate; frontend owns only two-second debounce/display | 01-04 | COVERED | Backend controls fully planned; frontend debounce remains excluded as deferred.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test adapters → production modules | Verification must distinguish fake seams from real provider/persistence boundaries. |
| protected HTTP response → browser | Safe public DTOs must remain aligned with actual OpenAPI documentation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-12 | Information Disclosure | verifier/router DTOs | high | mitigate | Assert explicit redaction against real route output and emitted OpenAPI, while fake adapters fail on live OAuth/network paths. |
| T-18-13 | Denial of Service | authoring admission tests | high | mitigate | Prove serial processing, cache hits, atomic daily limits, manual reserve, and retry delay behavior with deterministic clock fixtures. |
| T-18-14 | Tampering | stale asynchronous writes | high | mitigate | Exercise state version and summary digest compare-and-set rejection through production service seams. |
| T-18-SC | Tampering | npm installs | low | accept | No package installation is planned. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run build && npm run verify:episode-hashtag-authoring -- --focus=foundation`
- `npm run build && npm run verify:episode-hashtag-authoring -- --focus=lookup`
- `npm run build && npm run verify:episode-hashtag-authoring -- --focus=lifecycle`
- `npm run build && npm run verify:episode-hashtag-authoring -- --focus=route`
- `npm run typecheck && npm run build && npm run verify:episode-hashtag-authoring`
</verification>

<success_criteria>
- Every TRAILER-06 behavior has executable offline coverage with fake providers and temporary storage.
- Automated checks establish cache/quota safety and route/API confidentiality without a live network request.
- The phase is ready for execution with no unresolved source-audit item.
</success_criteria>

<output>
Create `.planning/phases/18-episode-hashtag-authoring/18-04-SUMMARY.md` when done
</output>
