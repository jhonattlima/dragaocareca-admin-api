---
phase: 16-draft-staging-and-private-youtube-job
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/verify-youtube-trailer-job-lifecycle.ts
  - package.json
autonomous: true
requirements: [TRAILER-02, TRAILER-03]
must_haves:
  truths:
    - Repository and worker implementation tasks have a compiled, runnable, offline fake-provider verification command before their changes begin.
    - The verifier foundation cannot access OAuth credentials, a YouTube channel, or network resources, and provides focused repository and worker invocation seams.
  artifacts:
    - src/scripts/verify-youtube-trailer-job-lifecycle.ts
    - package.json
  key_links:
    - The npm command compiles first and executes the development-only verifier against temporary fixtures.
    - Plans 16-02 and 16-03 depend on this plan and invoke their respective focused fake-provider checks.
---

<objective>
Create the offline fake-provider verification foundation before durable job repository or worker implementation.

Purpose: make every later job lifecycle task executable against a deterministic verifier instead of deferring integration proof to the final plan.
Output: compiled development verifier scaffold, focused invocation contract, and npm command.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-CONTEXT.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-RESEARCH.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the deterministic fake-provider verifier scaffold</name>
  <files>src/scripts/verify-youtube-trailer-job-lifecycle.ts</files>
  <behavior>
    - The compiled script rejects a non-development execution environment, creates and removes isolated temporary fixtures, and never reads YouTube/OAuth environment variables or calls a network API.
    - Its deterministic fake provider and `--focus=repository` / `--focus=worker` dispatcher establish runnable focused extension seams that Plans 16-02 and 16-03 must replace with repository and worker assertions as their exports arrive.
    - The default invocation documents the complete lifecycle scenarios reserved for Plan 16-04 and verifies only scaffold isolation until the later plans add production-contract assertions.
  </behavior>
  <action>Create the repository-native TypeScript verifier scaffold using Node assertions and temporary SQLite/media roots, following the existing compiled verifier conventions. Define an injected deterministic fake-provider interface for private-session, Range-resume, processing, cancellation, and normalized failure scenarios; expose runnable `--focus=repository` and `--focus=worker` branches that prove the isolated fake-provider contract and are explicitly extended by Plans 16-02 and 16-03 with real repository/worker assertions. Make the scaffold development-only, clean fixtures in `finally`, and explicitly prohibit network/OAuth credential use in its setup. This establishes offline verification for TRAILER-02 and TRAILER-03 before production job code exists.</action>
  <verify><automated>npm run build && node dist/scripts/verify-youtube-trailer-job-lifecycle.js --focus=repository && node dist/scripts/verify-youtube-trailer-job-lifecycle.js --focus=worker</automated></verify>
  <done>A compiled, deterministic, no-network fake-provider verifier exists with runnable focused repository and worker extension seams that later plans must strengthen with their real contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Register the compiled verifier command and enforce offline execution</name>
  <files>package.json</files>
  <action>Add `verify:youtube-trailer-job-lifecycle` beside existing compiled verifier scripts so it builds through the established npm workflow, runs the emitted development-only verifier with temporary SQLite/media roots, and forwards an optional focused argument. Configure the script without adding packages, live endpoints, OAuth credentials, or background-worker side effects. The command must remain usable by Plans 16-02 and 16-03 before their focused lifecycle exports are added.</action>
  <verify><automated>npm run build && npm run verify:youtube-trailer-job-lifecycle -- --focus=repository && npm run verify:youtube-trailer-job-lifecycle -- --focus=worker</automated></verify>
  <done>The npm command executes the compiled fake-provider verifier offline and provides the focused checks required by the next two waves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier fixture to future provider adapter | Test-only simulated provider outcomes must not cross into the live OAuth/provider boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-14 | I/E | fake-provider verifier | high | mitigate | The scaffold uses only deterministic injected fixtures, temporary roots, and a development-only guard; it reads no OAuth credential and calls no external endpoint. |
| T-16-15 | R/T | lifecycle verification foundation | medium | mitigate | Later repository and worker tasks replace their focused scaffold checks with assertions over real exports, preventing the final lifecycle proof from omitting either wiring boundary. |
| T-16-SC | T | package supply chain | low | accept | No package installation or package manifest dependency mutation is planned; package.json only receives a local verifier command. |
</threat_model>

<verification>
Run `npm run build && npm run verify:trailer-video-upload-lifecycle && npm run verify:youtube-trailer-job-lifecycle`. The YouTube verifier may only use fake-provider fixtures and temporary local state.
</verification>

<success_criteria>
TRAILER-02 and TRAILER-03 have an executable offline verification base before the durable repository and worker lifecycle are implemented.
</success_criteria>

<output>
Create `.planning/phases/16-draft-staging-and-private-youtube-job/16-05-SUMMARY.md` when done.
</output>
