---
phase: 13-return-zip-progress-to-user
plan: 05
type: execute
wave: 3
depends_on: [13-04]
files_modified:
  - src/docs/openapi.ts
  - src/scripts/verify-episode-artifact-downloads.ts
autonomous: true
gap_closure: true
requirements: [ZIP-01, ZIP-07]
must_haves:
  truths:
    - "OpenAPI accepts the fixed comma-separated selector lists accepted by the protected preparation route, including a representative multi-selector list."
    - "The compiled verifier enforces documentation-to-parser parity rather than only checking that the parameter is a string."
    - "The public contract retains the fixed Phase 12 selector allowlist and exposes no filesystem or cache internals."
  artifacts:
    - path: src/docs/openapi.ts
      provides: Accurate fixed-selector CSV parameter schema for preparation requests.
    - path: src/scripts/verify-episode-artifact-downloads.ts
      provides: OpenAPI and route-parser selector parity assertions.
  key_links:
    - from: OpenAPI prepare artifacts parameter
      to: parseEpisodeArtifactSelectors
      via: matching fixed CSV grammar and compiled representative values
      pattern: "episode,transcript"
---

<objective>
Repair the public preparation documentation so generated clients can send the same fixed CSV selector lists accepted by the route, and make the compiled verifier catch future drift.

Purpose: close the ZIP-07 contract mismatch without expanding the fixed artifact allowlist.
Output: an accurate OpenAPI selector schema plus executable documentation/parser parity coverage.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-return-zip-progress-to-user/13-CONTEXT.md
@.planning/phases/13-return-zip-progress-to-user/13-RESEARCH.md
@.planning/phases/13-return-zip-progress-to-user/13-VERIFICATION.md
@.planning/phases/13-return-zip-progress-to-user/13-03-SUMMARY.md
@src/docs/openapi.ts
@src/services/episode-artifact-download.service.ts
@src/scripts/verify-episode-artifact-downloads.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Express the fixed selector CSV grammar in OpenAPI</name>
  <files>src/docs/openapi.ts</files>
  <action>Update the POST preparation operation’s `artifacts` query parameter so its schema describes one nonempty comma-separated list composed only of the existing fixed selectors, rather than an enum that permits only one selector. Keep the documented default/all-artifacts behavior, normalized catalog-order semantics, bearer security, no-store headers, and public-only status schema. Use an anchored OpenAPI-compatible pattern or equivalent schema that accepts `episode,transcript` and the other valid fixed-selector combinations while excluding arbitrary values. This implements D-01, D-07, D-10, and D-11.</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run verify:episode-artifact-downloads</automated>
  </verify>
  <done>The documented prepare parameter accepts valid fixed CSV selector lists and continues to exclude paths and selectors outside the allowlist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce OpenAPI selector parity in the compiled verifier</name>
  <files>src/scripts/verify-episode-artifact-downloads.ts</files>
  <behavior>
    - The verifier confirms that a representative CSV accepted by parseEpisodeArtifactSelectors is accepted by the documented OpenAPI schema.
    - The verifier confirms the actual POST route accepts that same CSV and returns normalized selectors in catalog order.
    - The verifier rejects a selector value outside the fixed catalog through both parser-oriented and OpenAPI schema assertions.
  </behavior>
  <action>Replace the shallow OpenAPI string-type assertion with executable parity checks. Extract the documented preparation parameter schema, evaluate its declared grammar against representative valid and invalid values, and compare valid values with parseEpisodeArtifactSelectors plus an actual direct-router POST invocation. Verify `episode,transcript` is accepted and normalized according to the existing catalog, while arbitrary selectors remain excluded. Keep all checks offline against compiled exports; do not add a test framework, HTTP listener, package, or duplicate selector catalog. This implements D-01, D-05, D-10, and D-11.</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run verify:episode-artifact-downloads</automated>
  </verify>
  <done>The compiled verifier fails if OpenAPI no longer accepts a route-valid CSV or documents selectors beyond the fixed parser contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API documentation/client generation → protected prepare route | Untrusted client serialization must remain constrained to the server-defined selector vocabulary. |
| request selector CSV → final artifact preflight | A documented selector value must resolve only through the existing fixed catalog. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-10 | Tampering | OpenAPI artifacts parameter | medium | mitigate | Document the same fixed CSV grammar as the parser and execute parity checks against representative values. |
| T-13-11 | Elevation of Privilege | generated API clients | medium | mitigate | Preserve bearer security and prevent documentation from implying arbitrary selector or path input is valid. |
| T-13-SC | Tampering | package supply chain | low | accept | No package installation or dependency change is in this closure plan. |
</threat_model>

<verification>
The compiled verifier must prove a representative valid CSV works through the schema, parser, and direct route, then prove an outside selector is rejected. Run typecheck and the focused compiled verifier after documentation changes.
</verification>

<success_criteria>
ZIP-07 has one accurate public selector contract: OpenAPI and the protected route agree on accepted fixed CSV lists, normalized output, and allowlist boundaries.
</success_criteria>

## Artifacts

- `src/docs/openapi.ts` — fixed-selector CSV schema for the prepare operation.
- `src/scripts/verify-episode-artifact-downloads.ts` — OpenAPI/parser/route parity coverage.

<output>
Create `.planning/phases/13-return-zip-progress-to-user/13-05-SUMMARY.md` when done.
</output>
