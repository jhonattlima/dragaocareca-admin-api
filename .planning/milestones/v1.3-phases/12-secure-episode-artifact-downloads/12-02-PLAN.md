---
phase: 12-secure-episode-artifact-downloads
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - package.json
  - package-lock.json
  - src/scripts/verify-episode-artifact-downloads.ts
autonomous: true
requirements: [ART-07]
must_haves:
  truths:
    - "A compiled repository-native verifier and its npm command exist and run before selector parsing or final-file preflight is introduced."
  artifacts:
    - path: "src/scripts/verify-episode-artifact-downloads.ts"
      provides: "Runnable compiled verifier scaffold for progressive Phase 12 contract checks"
    - path: "package.json"
      provides: "Approved runtime Archiver dependency"
    - path: "package-lock.json"
      provides: "Package-manager generated dependency graph"
  key_links:
    - from: "package.json verify script"
      to: "compiled verifier scaffold"
      via: "NODE_ENV=development dist/scripts execution"
---

<objective>
Install the approved ZIP library and bootstrap the compiled verifier before security-critical artifact selection is implemented.

Purpose: The repository-native verifier must be executable before subsequent plans introduce the selector/preflight boundary and route behavior it will validate.
Output: `archiver` dependency plus a runnable verifier scaffold and npm script; no selector parsing, filesystem preflight, or route behavior yet.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-secure-episode-artifact-downloads/12-CONTEXT.md
@.planning/phases/12-secure-episode-artifact-downloads/12-RESEARCH.md
@.planning/phases/12-secure-episode-artifact-downloads/12-PATTERNS.md
@.planning/phases/12-secure-episode-artifact-downloads/12-VALIDATION.md

<interfaces>
Follow the compiled verifier convention in `src/scripts/verify-public-episodes.ts` and the script conventions in `package.json`. The scaffold must remain dependency-free apart from Node built-ins and must not attempt to invoke a route or service that does not exist yet.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the human-approved Archiver runtime dependency</name>
  <read_first>`.planning/phases/12-secure-episode-artifact-downloads/12-01-PLAN.md`, `.planning/phases/12-secure-episode-artifact-downloads/12-RESEARCH.md` (Package Legitimacy Audit), `package.json`, `package-lock.json`</read_first>
  <files>package.json, package-lock.json</files>
  <action>After the Plan 12-01 human approval signal, run the package manager to add `archiver` as a runtime dependency and accept its generated lockfile update; never edit the lockfile manually. Run `npm run typecheck` immediately after installation. Only when that compiler output proves declarations are missing or incompatible, add `@types/archiver` as a development dependency under the checkpoint's conditional approval; otherwise leave the type package absent. Preserve all existing scripts and dependencies.</action>
  <verify><automated>npm run typecheck &amp;&amp; npm ls archiver</automated></verify>
  <done>Acceptance criteria: `archiver` is a declared runtime dependency with a package-manager-generated lockfile; TypeScript passes with no unnecessary `@types/archiver` package added.</done>
</task>

<task type="auto">
  <name>Task 2: Create and run the compiled verifier scaffold before parser implementation</name>
  <read_first>`src/scripts/verify-public-episodes.ts`, `package.json`, `.planning/phases/12-secure-episode-artifact-downloads/12-VALIDATION.md`, `.planning/phases/12-secure-episode-artifact-downloads/12-RESEARCH.md` (Open Questions (RESOLVED))</read_first>
  <files>src/scripts/verify-episode-artifact-downloads.ts, package.json</files>
  <action>Create a dependency-free compiled verifier scaffold with a `main` entry point that confirms its own execution context and reports that progressive contract assertions are ready. Add `verify:episode-artifact-downloads` to `package.json` using the existing `NODE_ENV=development node dist/scripts/...` convention. Build and run the scaffold in this plan, before any selector parser, filesystem preflight, route, or ZIP-entry assertion is introduced. Reserve the in-process central-directory reader for its later verifier-only implementation per the resolved research decision; if that reader proves insufficient, stop for a new blocking human legitimacy checkpoint before considering any ZIP-reader package.</action>
  <verify><automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run verify:episode-artifact-downloads</automated></verify>
  <done>Acceptance criteria: the compiled verifier file and npm command exist, compile, and run successfully before Plan 12-03 changes the selector/preflight boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client query → selector parser | Untrusted query values determine which catalog entries may be considered. |
| catalog → local storage | Approved selector entries resolve to server-side paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information Disclosure / Tampering | artifact parser and preflight service | high | mitigate | Fixed typed catalog, singular strict CSV parser, final-path helper only, and regular-file preflight prevent traversal and non-final artifact inclusion. |
| T-12-03 | Denial of Service | malformed selector input | medium | mitigate | Reject invalid query shapes before filesystem reads and avoid directory enumeration. |
| T-12-SC | Tampering | npm dependency install | high | mitigate | Plan 12-01 approval gates the exact package installation; generated lockfile records the reviewed dependency graph. |
</threat_model>

<verification>
Run `npm run typecheck && npm run build && npm run verify:episode-artifact-downloads`. This bootstrap must complete before Plan 12-03 implements the security-critical parser/preflight boundary. Assumption-delta result: no-change — this phase adds no new primary identity model. External API coverage matrix: not applicable; no external API is integrated.
</verification>

<success_criteria>
The approved archive dependency is installed and the repository-native compiled verifier is demonstrably runnable before the artifact-selection security boundary is introduced.
</success_criteria>

<output>
Create `.planning/phases/12-secure-episode-artifact-downloads/12-02-SUMMARY.md` when done.
</output>
