---
phase: 12-secure-episode-artifact-downloads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [ART-01, ART-02, ART-03, ART-04, ART-05, ART-06, ART-07]
must_haves:
  truths:
    - "The suspicious archive dependency is explicitly approved by a human before installation."
  artifacts:
    - path: ".planning/phases/12-secure-episode-artifact-downloads/12-01-PLAN.md"
      provides: "Documented blocking package-legitimacy gate"
  key_links:
    - from: "human legitimacy approval"
      to: "archiver package installation"
      via: "Plan 12-02 dependency"
---

<objective>
Obtain the required package-legitimacy approval before adding the ZIP implementation dependency.

Purpose: The research package audit classifies `archiver` (and conditionally `@types/archiver`) as suspicious, so dependency provenance must be reviewed before code uses it.
Output: A blocking human decision that authorizes the exact installation path for Plan 12-02.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-secure-episode-artifact-downloads/12-CONTEXT.md
@.planning/phases/12-secure-episode-artifact-downloads/12-RESEARCH.md
@.planning/phases/12-secure-episode-artifact-downloads/12-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve the flagged Archiver dependency before installation</name>
  <files>.planning/phases/12-secure-episode-artifact-downloads/12-01-PLAN.md</files>
  <action>Pause before any package-manager mutation and present the research audit for human provenance review. This task authorizes dependency installation only; it does not add a dependency, modify source code, or change the existing authentication model.</action>
  <what-built>Planning is complete; no dependency has been installed yet.</what-built>
  <how-to-verify>
    1. Review the Package Legitimacy Audit in `12-RESEARCH.md`: the target runtime package is `archiver` from the documented `archiverjs/node-archiver` source.
    2. Confirm that its intended use is limited to explicit, preflighted final artifact files in the protected episode download route; recursive directory archiving is not permitted.
    3. Approve installation of `archiver`. Also approve conditional installation of `@types/archiver` only if `npm run typecheck` after the Archiver install proves that declarations are missing or incompatible.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize this exact package path, or describe the package concern.</resume-signal>
  <verify><automated>test -f .planning/phases/12-secure-episode-artifact-downloads/12-01-PLAN.md</automated></verify>
  <done>Acceptance criteria: an explicit approval signal is recorded before either flagged dependency can be installed; no installation occurs before that signal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → build dependency graph | A third-party archive package enters the production dependency graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-SC | Tampering | npm dependency installation | high | mitigate | Require this blocking human provenance review before Plan 12-02 installs `archiver`; install `@types/archiver` only if compiler output requires it and the same approval covers that conditional package. |
</threat_model>

<verification>
Human approval is recorded through the checkpoint before any package-manager mutation. External API coverage matrix: not applicable; this phase integrates no external API.
</verification>

<success_criteria>
The executor has an explicit approval signal before adding either flagged package; the type package remains conditional on an actual compiler need.
</success_criteria>

<output>
Create `.planning/phases/12-secure-episode-artifact-downloads/12-01-SUMMARY.md` when done.
</output>
