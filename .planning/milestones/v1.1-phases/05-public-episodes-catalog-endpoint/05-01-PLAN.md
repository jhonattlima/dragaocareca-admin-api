---
phase: 05-public-episodes-catalog-endpoint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/public-episodes.routes.ts
  - src/services/public-episode-catalog.service.ts
  - src/docs/openapi.ts
autonomous: true
requirements:
  - CATALOG-03
  - CATALOG-04
must_haves:
  truths:
    - The public catalog contract is a plain JSON array that already matches home-page card, search, and navigation needs per D-01 and D-07.
    - Each catalog item exposes backend-owned absolute page and media URLs instead of raw filenames or frontend-built paths per D-04 and D-05.
    - Guest search data is normalized so frontend consumers can keep matching `guest.name` without legacy shape assumptions per D-07.
  artifacts:
    - src/routes/public-episodes.routes.ts
    - src/services/public-episode-catalog.service.ts
    - src/docs/openapi.ts
  key_links:
    - Public route -> catalog mapper/service -> published repository rows
    - Catalog mapper -> backend URL rules for page, audio, cover, and trailer fields
---

<objective>
Define the executable public catalog contract and the mapper boundary that converts canonical episode rows into frontend-ready catalog items for Phase 5.

Purpose: Lock the response shape to current frontend usage while keeping publication and URL rules in the backend per D-01, D-04, D-05, and D-07.
Output: A new public catalog route file, a dedicated DTO mapping service, and OpenAPI documentation for the array response.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.1-phases/05-public-episodes-catalog-endpoint/05-CONTEXT.md
@.planning/milestones/v1.1-phases/05-public-episodes-catalog-endpoint/05-RESEARCH.md
@.planning/PROJECT.md
@src/routes/feed.routes.ts
@src/docs/openapi.ts
@src/database/repositories/episode.repository.ts
@../dragaocareca_frontend/src/config.js
@../dragaocareca_frontend/src/components/PageHome/EpisodeGrid/EpisodeGrid.js
@../dragaocareca_frontend/src/components/PageHome/EpisodeGrid/EpisodeCard/EpisodeCard.js
@../dragaocareca_frontend/src/components/PageHome/EpisodeGrid/SearchBar/SearchBar.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the public catalog DTO mapper and URL policy</name>
  <files>src/services/public-episode-catalog.service.ts</files>
  <action>Create `src/services/public-episode-catalog.service.ts` as the single Phase 5 mapping boundary. Define the public catalog item type as a plain-array item per D-01 and keep it limited to the explicit Phase 5 frontend contract per D-07: `episodeId` as the stable identifier for card/navigation compatibility, `title`, `pubDate`, `guests` normalized to `{ name }[]` for search compatibility, `pageUrl`, `audioUrl`, `coverUrl`, and `trailerUrl` as backend-owned absolute URLs; include `summary` only if it remains additive and does not replace any required field. Centralize URL assembly per D-04 and D-05, use the resolved trailer fallback of request origin plus `/media/${trailerFileName}`, prefer low cover assets when present, and return `null` instead of leaking raw filename fields. Do not pull publication filtering, sorting, auth, or Telegram behavior into this service.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The mapper/service file exists, exposes the required Phase 5 compatibility fields (`episodeId`, `title`, `pubDate`, `guests[].name`, `pageUrl`, `audioUrl`, `coverUrl`, `trailerUrl`), and makes backend ownership of public URL fields explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Add the public catalog route file on the thin-router pattern</name>
  <files>src/routes/public-episodes.routes.ts</files>
  <action>Create `src/routes/public-episodes.routes.ts` on the same thin public-route pattern as `src/routes/feed.routes.ts`. Add `GET /` only. The handler must read from `episodeRepository.listPublished(new Date())` and map those rows through the new catalog service so D-02, D-03, and D-06 remain backend-owned. Keep the route unauthenticated, keep ordering as returned by the repository, and do not add pagination, feed logic, admin concerns, or unrelated route behavior.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>A dedicated public router exists for the catalog endpoint and its only data source is the published repository read plus the catalog mapper.</done>
</task>

<task type="auto">
  <name>Task 3: Document the array response in OpenAPI</name>
  <files>src/docs/openapi.ts</files>
  <action>Extend `src/docs/openapi.ts` with a `PublicEpisodeCatalogItem` schema and a documented `GET /v1/public/episodes` path. The schema must describe the plain JSON array response per D-01, the published-home ordering contract per D-03, and backend-owned absolute URL fields per D-04 and D-05. Keep this update limited to the public catalog endpoint; do not rename or reshape unrelated admin or feed schemas.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`/docs` and `/docs.json` have a concrete public catalog contract that matches the Phase 5 route and mapper decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> public catalog route | Untrusted anonymous callers can request catalog data |
| public route -> repository | The route must not broaden reads beyond published episodes |
| public mapper -> public URL contract | Absolute links must not leak internal filenames or drift into frontend-owned path logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `src/routes/public-episodes.routes.ts` | high | mitigate | Restrict the route to `episodeRepository.listPublished(new Date())` only per D-02; do not call broader episode reads. |
| T-05-02 | Tampering | `src/services/public-episode-catalog.service.ts` | medium | mitigate | Centralize URL assembly per D-04 and D-05 so callers cannot reconstruct or override page/media paths from raw filenames. |
| T-05-03 | Repudiation | `src/docs/openapi.ts` | low | mitigate | Keep the documented schema aligned with the shipped contract so frontend integration can verify the exact public fields. |
| T-05-SC | Tampering | npm installs | low | accept | No package installation is planned for Phase 5; reuse the existing dependency set only. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run build`
</verification>

<success_criteria>
- `GET /v1/public/episodes` is planned as a plain JSON array contract, not an envelope.
- Catalog item fields are explicitly mapped from backend episode rows to frontend-ready public DTOs.
- OpenAPI documents the endpoint and the public catalog item shape without touching unrelated endpoint contracts.
</success_criteria>

<output>
Create `.planning/milestones/v1.1-phases/05-public-episodes-catalog-endpoint/05-01-SUMMARY.md` when done
</output>
