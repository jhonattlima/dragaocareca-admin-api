---
phase: 16-draft-staging-and-private-youtube-job
plan: "06"
type: execute
wave: 4
depends_on: [16-03]
files_modified: []
autonomous: false
requirements: [TRAILER-02, TRAILER-03]
user_setup:
  - service: YouTube Data API OAuth
    why: Live private uploads require a production refresh-token grant for the intended channel and a human-approved operational envelope.
    env_vars:
      - name: YOUTUBE_TRAILER_JOB_ENABLED
        source: "Production environment; leave false until this checkpoint is approved"
      - name: YOUTUBE_TRAILER_JOB_POLL_INTERVAL_MS
        source: "Production environment; documented default 60000"
      - name: YOUTUBE_TRAILER_JOB_REQUEST_TIMEOUT_MS
        source: "Production environment; documented default 30000"
      - name: YOUTUBE_TRAILER_JOB_RETRY_INITIAL_MS
        source: "Production environment; documented default 60000"
      - name: YOUTUBE_TRAILER_JOB_RETRY_MAX_MS
        source: "Production environment; documented default 900000"
      - name: YOUTUBE_TRAILER_JOB_MAX_RETRIES
        source: "Production environment; documented default 5"
must_haves:
  truths:
    - Live provider work remains disabled unless a human has validated the production OAuth scope, refresh token, intended channel, and conservative poll/retry settings.
    - Automated verification remains fully offline and never uploads to or otherwise calls a live YouTube provider.
  artifacts:
    - src/config/env.ts
    - .env.example
  key_links:
    - The worker start path checks the default-false enable flag before any provider activity.
    - The operator approval records the accepted production limits before enabling the worker.
---

<objective>
Block live YouTube worker enablement until an operator approves the production OAuth/channel readiness and conservative operational limits.

Purpose: resolve deployment-only readiness without weakening the offline fake-provider test boundary.
Output: explicit human approval or a documented issue list; no source-code change is performed by this checkpoint.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-CONTEXT.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-RESEARCH.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-VALIDATION.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve production OAuth/channel readiness and live-worker limits</name>
  <files>none — operator checkpoint only</files>
  <action>Keep the live-worker enable flag false until the operator completes this approval. This checkpoint changes no repository file and does not authorize automated live-provider calls.</action>
  <what-built>The Plan 16-03 worker is opt-in and defaults to disabled; its offline fake-provider verifier has already proven the local lifecycle without live provider access.</what-built>
  <how-to-verify>
    1. Keep `YOUTUBE_TRAILER_JOB_ENABLED=false` while reviewing the production OAuth client, refresh token, and selected channel through operator-controlled Google/YouTube access.
    2. Confirm the refresh-token grant is authorized for server-side `youtube.upload` and belongs to the intended production channel; do not upload a test video for this checkpoint.
    3. Record and configure the conservative operational envelope: one worker, 60000 ms processing poll, 30000 ms request timeout, 60000 ms initial retry, exponential retry cap of 900000 ms, and five maximum retries. If production needs different values, record the conservative approved overrides and why.
    4. Verify the deployed environment still has the worker disabled before approval. Enable it only after all prior checks pass; otherwise leave it disabled and report the missing grant, channel mismatch, or limit concern.
  </how-to-verify>
  <verify><automated>npm run build && npm run verify:trailer-video-upload-lifecycle && npm run verify:youtube-trailer-job-lifecycle</automated></verify>
  <done>The intended production OAuth grant, refresh token, channel, and conservative operational settings have human approval before the enable flag changes from false.</done>
  <resume-signal>Type "approved" only after the OAuth/token/channel and limits are recorded and the enable flag is intentionally set; otherwise describe the blocker while leaving the worker disabled.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-controlled OAuth configuration to live worker | Production credentials and channel authorization are external state that cannot be established by repository fixtures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-16 | E/I | production OAuth grant and channel | critical | mitigate | Default-false worker enablement plus this blocking human gate validates the upload-authorized grant and intended channel before any live transfer. |
| T-16-17 | D | VPS worker operations | high | mitigate | The checkpoint configures and records one-worker, poll, timeout, and capped-retry limits before allowing live work. |
| T-16-SC | T | package supply chain | low | accept | This checkpoint installs no package and changes no package manifest. |
</threat_model>

<verification>
Automated evidence remains `npm run build && npm run verify:trailer-video-upload-lifecycle && npm run verify:youtube-trailer-job-lifecycle`; no automated test may call a live provider. Human approval is required before enabling live work.
</verification>

<success_criteria>
TRAILER-02 and TRAILER-03 cannot activate a live provider worker until the production grant/channel and conservative operational configuration have been explicitly approved, while all automated lifecycle verification stays offline.
</success_criteria>

<output>
Create `.planning/phases/16-draft-staging-and-private-youtube-job/16-06-SUMMARY.md` when done.
</output>
