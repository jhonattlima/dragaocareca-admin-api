---
phase: 16-draft-staging-and-private-youtube-job
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/verify-trailer-video-upload-lifecycle.ts
  - src/routes/episodes.routes.ts
  - src/services/episode-draft-reservation.service.ts
  - src/services/episode-trailer-video.service.ts
  - src/database/repositories/episode.repository.ts
autonomous: true
requirements: [TRAILER-02, TRAILER-03, TRAILER-09]
must_haves:
  truths:
    - A draft reservation belongs to one authenticated owner and episode for 24 hours, stages only server-derived MP4 media, and abandoned staging is cleaned.
    - The matching authenticated episode creation promotes a staged trailer atomically, while every create/promotion/consume failure compensates without exposing a partial final or losing a prior final artifact.
  artifacts:
    - src/services/episode-draft-reservation.service.ts
    - src/routes/episodes.routes.ts
    - src/scripts/verify-trailer-video-upload-lifecycle.ts
  key_links:
    - The protected create route passes authenticated owner and draft ID through reservation validation before promotion and consumes only after durable success.
    - The trailer replacement rollback routine is used only with server-derived staging and restores prior final media before the route reports a failure.
---

<objective>
Harden the already-present reservation/staging implementation into an atomic, compensating New Episode create workflow.

Purpose: fulfill the irreversible media-promotion safety boundary before a private upload job can depend on canonical final media.
Output: fault-injected lifecycle proof and an owner-bound reservation/create/promotion/cleanup workflow.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-CONTEXT.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-RESEARCH.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-VALIDATION.md
@.planning/phases/15-final-trailer-video-artifact/15-04-SUMMARY.md

<interfaces>
From `src/services/episode-draft-reservation.service.ts`: preserve `reserveTrailerVideoDraft`, `checkTrailerVideoDraft`, `consumeTrailerVideoDraft`, and `cleanupExpiredTrailerVideoDrafts` as the owner/expiry boundary.
From `src/services/episode-trailer-video.service.ts`: preserve `replaceEpisodeTrailerVideo(episodeId, stagedFilePath)` as the canonical final-file rollback boundary.
From `src/routes/episodes.routes.ts`: `/drafts`, `/:episodeId/trailer-video`, and protected `POST /` already carry the in-progress draft ID contract; improve them in place rather than replacing their surface.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the compiled draft lifecycle verifier with compensation cases</name>
  <files>src/scripts/verify-trailer-video-upload-lifecycle.ts</files>
  <behavior>
    - A wrong owner, expired reservation, invalid staged input, disconnect/cleanup, and stale draft leave no final trailer claim.
    - Injected create, promotion, media-metadata, reservation-consume, and post-create-side-effect failures preserve the last-known-good final file, remove any newly created episode row, and leave staging/reservation in the documented retryable or cleaned terminal state.
    - The direct protected router stack proves the final success path consumes the exact reservation only after final media/metadata are durable.
  </behavior>
  <action>Extend the existing isolated SQLite/media verifier before production changes. Exercise real route and service seams with in-memory multipart input and temporary server-derived media roots; add deterministic fault-injection seams around every atomic create/promotion/consume boundary and assert SQLite row, reservation state, staging file, and canonical final-file outcomes. Preserve the existing no-network and no-live-provider setup; cite D-01, D-02, and D-03 in verifier scenario names or assertions.</action>
  <verify><automated>npm run build && npm run verify:trailer-video-upload-lifecycle</automated></verify>
  <done>The compiled verifier fails on a broken compensation path and passes only when all reservation ownership, expiry cleanup, atomic promotion, and rollback outcomes required by D-01 through D-03 are proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make draft consumption and final promotion a recoverable create unit</name>
  <files>src/routes/episodes.routes.ts, src/services/episode-draft-reservation.service.ts, src/services/episode-trailer-video.service.ts, src/database/repositories/episode.repository.ts</files>
  <behavior>
    - A reservation is normalized-owner-bound, episode-bound, and valid for exactly the allowed state/expiry before staging or create.
    - A matching authenticated Save creates/persists the episode, promotes only the expected staged MP4 through rollback-safe media logic, then consumes the reservation; any later failure compensates the new row/final file and does not consume a retryable reservation.
    - Existing-episode replacement continues to use the final-video path and does not make staged draft media publicly final.
  </behavior>
  <action>Modify the existing reservation, route, repository, and final-video service as one recovery protocol, not a new upload flow. Implement D-01 owner-bound 24-hour reservation issuance and abandoned staging cleanup, D-02 matching-owner consumption after durable canonical promotion, and D-03 explicit compensation ordering for create, promotion, metadata, consumption, and follow-on failure. Keep all filesystem targets derived from the episode ID/media kind; retain the current final artifact until rollback succeeds; preserve `AUTH_BYPASS` as the same normalized owner identity; do not move any scheduling/feed rule or provider behavior into this task.</action>
  <verify><automated>npm run typecheck && npm run build && npm run verify:trailer-video-upload-lifecycle</automated></verify>
  <done>The existing draft endpoints and create endpoint provide an atomic observable finalization result, while every verifier-injected failure leaves no partially finalized draft and protects last-known-good media.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client to draft routes | Untrusted episode ID, multipart MP4, and draft token meet owner/expiry controls. |
| draft staging to final media | Filesystem state becomes canonical only through server-derived promotion and compensation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | E/T | draft reservation and create route | high | mitigate | D-01/D-02 owner, episode, state, and expiry checks precede staging/promotion; authenticated owner is normalized consistently. |
| T-16-02 | T/D | promotion rollback | high | mitigate | D-03 requires rollback-safe final replacement and compensating row/reservation cleanup proved with injected failures. |
| T-16-03 | I | route response and media layout | medium | mitigate | Use server-derived paths and existing sanitized response fields; do not return staging or absolute filesystem locations. |
| T-16-SC | T | package supply chain | low | accept | No package installation or package manifest mutation is planned in this plan. |
</threat_model>

<verification>
Run `npm run typecheck && npm run build && npm run verify:trailer-video-upload-lifecycle`. The verifier must execute offline against temporary SQLite/media fixtures and direct protected router stacks.
</verification>

<success_criteria>
TRAILER-09 is objectively covered: an owner-bound staged draft becomes canonical only through the matching authenticated Save, and injected failure/cancellation/expiry paths preserve canonical media and truthful state.
</success_criteria>

<output>
Create `.planning/phases/16-draft-staging-and-private-youtube-job/16-01-SUMMARY.md` when done.
</output>
