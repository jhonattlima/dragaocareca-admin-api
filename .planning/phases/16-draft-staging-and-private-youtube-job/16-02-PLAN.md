---
phase: 16-draft-staging-and-private-youtube-job
plan: "02"
type: execute
wave: 2
depends_on: [16-05]
files_modified:
  - src/database/sqlite.ts
  - src/database/repositories/youtube-trailer-job.repository.ts
  - src/scripts/verify-youtube-trailer-job-lifecycle.ts
autonomous: true
requirements: [TRAILER-02, TRAILER-03, TRAILER-09]
must_haves:
  truths:
    - Starting for the same finalized source returns one durable active job instead of creating duplicate work.
    - Job storage records source evidence, private transfer/reconciliation state, cancellation/recovery evidence, and lease/revision ownership without storing client-supplied media paths.
  artifacts:
    - src/database/repositories/youtube-trailer-job.repository.ts
    - src/database/sqlite.ts
  key_links:
    - Active-source queries and conditional updates use episode ID, source fingerprint, and lease/revision predicates so stale worker results cannot commit.
    - Schema initialization creates idempotent tables/indexes before the worker starts.
---

<objective>
Establish the durable SQLite source-of-truth and compare-and-set repository for private YouTube jobs.

Purpose: make job creation, cancellation, recovery, and replacement safe across duplicate API calls and process restart before any provider call is possible.
Output: idempotent SQLite migration and typed repository contract for source-fingerprinted lifecycle transitions.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-CONTEXT.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-RESEARCH.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-PATTERNS.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-VALIDATION.md

<interfaces>
Follow `src/database/repositories/artifact-job.repository.ts` for typed row mapping and conditional status changes. Follow `src/database/sqlite.ts` `ensureArtifactJobColumns`/`ensureEpisodeTrailerVideoDraftTable` for idempotent schema compatibility. This plan exports the job row/status/claim/repository operations consumed by Plans 16-03 and 16-04.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the persisted YouTube job state and idempotent SQLite migration</name>
  <files>src/database/sqlite.ts</files>
  <behavior>
    - New and existing databases receive a compatible YouTube job table and indexes without data loss.
    - Each row can preserve canonical relative source evidence, session/provider references, transfer/processing progress, retry/error records, cancellation/obsolete timestamps, and lease/revision data.
    - The schema allows only documented private-job lifecycle states and indexes active source/recovery selection efficiently.
  </behavior>
  <action>Add an idempotent `youtube_trailer_jobs` schema initializer and compatibility helpers following the existing SQLite migration conventions. Model immutable source identity as canonical relative filename plus SHA-256 and byte count; model mutable state separately with worker lease/revision, session URI, confirmed bytes, provider video/status/processing details, safe error category, retry timing, cancellation boundary, obsoletion, and timestamps. Add indexes/constraints that support one active job for the current source without relying on browser values. This is D-08 durable state; do not store credentials or absolute paths.</action>
  <verify><automated>npm run typecheck && npm run build && npm run verify:youtube-trailer-job-lifecycle -- --focus=repository</automated></verify>
  <done>Application startup can idempotently initialize a durable job lifecycle schema with source identity and concurrency fields sufficient for safe recovery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement typed job repository idempotency and lease-guarded transitions</name>
  <files>src/database/repositories/youtube-trailer-job.repository.ts, src/scripts/verify-youtube-trailer-job-lifecycle.ts</files>
  <behavior>
    - A lookup/create for the same episode/source evidence returns the active job, while a changed source can be represented separately and older work can be obsoleted.
    - Claim, heartbeat, cancel, retry, reconcile, and terminal updates require expected source/state plus lease or revision, rejecting late writers.
    - Repository reads expose internal records only to services, not HTTP DTOs.
  </behavior>
  <action>Create the repository using the artifact-job repository's typed mapping and SQLite conditional-update pattern. Export explicit operations for source-fingerprint creation/reuse, active lookup, worker claim/release, revision/lease-guarded progress/provider/error updates, cancellation request, retry eligibility, startup recovery selection, and obsoleting prior source work. Extend the Wave 1 `--focus=repository` verifier branch to exercise the real repository against temporary SQLite and prove duplicate-source coalescing plus rejected stale lease/revision writes. Enforce D-08 persistence and D-04 stale-result suppression at SQL predicates; make no provider/network calls and expose no browser DTO mapping here.</action>
  <verify><automated>npm run typecheck && npm run build && npm run verify:youtube-trailer-job-lifecycle -- --focus=repository</automated></verify>
  <done>A later service can deterministically create/reuse exactly one active source job and reject stale conditional writes after replacement, cancellation, or worker lease loss.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service to SQLite job store | Durable records control whether a provider operation is initiated or a late result is accepted. |
| replacement to existing job state | A same-path final file can represent a different source and must invalidate old work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-16-04 | T/R | job repository | high | mitigate | Source SHA-256/bytes plus conditional lease/revision transitions make stale or forged completion writes fail. |
| T-16-05 | D | active job creation | medium | mitigate | Indexed active-source lookup and durable claims coalesce duplicate starts into one job. |
| T-16-06 | I | persisted job data | high | mitigate | Keep session URI/provider detail internal; database stores no OAuth credential and no client-visible filesystem path. |
| T-16-SC | T | package supply chain | low | accept | No package installation or package manifest mutation is planned in this plan. |
</threat_model>

<verification>
Run `npm run typecheck && npm run build && npm run verify:youtube-trailer-job-lifecycle -- --focus=repository`; the Wave 1 verifier foundation is expanded by this plan and later runs in the full suite after the wave.
</verification>

<success_criteria>
TRAILER-02 and TRAILER-03 have a durable, source-fingerprinted coordination point that supports private-first job idempotency, retry/recovery, and stale-write suppression.
</success_criteria>

<output>
Create `.planning/phases/16-draft-staging-and-private-youtube-job/16-02-SUMMARY.md` when done.
</output>
