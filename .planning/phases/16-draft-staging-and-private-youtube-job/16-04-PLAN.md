---
phase: 16-draft-staging-and-private-youtube-job
plan: "04"
type: execute
wave: 3
depends_on: [16-01, 16-02, 16-03]
files_modified:
  - src/routes/episodes.routes.ts
  - src/docs/openapi.ts
  - src/scripts/verify-youtube-trailer-job-lifecycle.ts
  - src/scripts/verify-trailer-video-upload-lifecycle.ts
  - package.json
autonomous: true
requirements: [TRAILER-02, TRAILER-03, TRAILER-09]
must_haves:
  truths:
    - Authenticated operators can start, read status for, and request cancellation of a private job through protected DTOs that never reveal provider credentials, resumable session locations, raw provider failures, or filesystem paths.
    - Compiled fake-provider verification proves private initiation, resume/reconciliation, processing, cancel/retry/restart/replacement behavior, single active source job, and sanitized route/OpenAPI contracts without a live provider call.
  artifacts:
    - src/routes/episodes.routes.ts
    - src/docs/openapi.ts
    - src/scripts/verify-youtube-trailer-job-lifecycle.ts
    - package.json
  key_links:
    - Start/status/cancel routes call service DTO mappers rather than repository/provider objects.
    - The build-output verifier injects a fake provider into actual service/worker and direct protected route stacks.
---

<objective>
Expose the protected private-job contract and prove the complete draft/job lifecycle offline with a compiled fake provider.

Purpose: make the user-facing lifecycle safe to poll and operationally verifiable without channel credentials, live calls, or accidental Phase 17 scope.
Output: protected start/status/cancel routes, safe OpenAPI DTOs, and exhaustive compiled lifecycle verifiers.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-CONTEXT.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-RESEARCH.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-VALIDATION.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-01-SUMMARY.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-02-SUMMARY.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-03-SUMMARY.md

<interfaces>
Plan 16-03 must provide service start/status/cancel DTO mappers and an injectable provider/worker startup seam. Routes use `requireAuth`, positive integer/Zod validation, and `Cache-Control: no-store` before lifecycle authentication following artifact-job routes. The verifier compiles to `dist/scripts` and runs under temporary SQLite/media roots like existing lifecycle scripts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add protected private-job start, status, and cancel contracts</name>
  <files>src/routes/episodes.routes.ts, src/docs/openapi.ts</files>
  <behavior>
    - Missing authentication is rejected; positive episode/job identifiers and strict request DTOs are enforced with no-store responses.
    - Start returns an existing active current-source job or one newly queued job; status and cancel use opaque job IDs and only safe lifecycle/progress/cancellation/error fields.
    - Route and OpenAPI payloads exclude credential material, resumable session locations, raw provider responses/errors, and filesystem paths; they never offer public publish/update behavior.
  </behavior>
  <action>Add protected episode-scoped start/status/cancel endpoints adjacent to the existing artifact-job lifecycle routes. Apply `requireAuth`, strict Zod validation, and no-store response headers before auth/validation; delegate all lifecycle work to Plan 16-03 service methods and map only its sanitized DTO. Document the same safe request/response/error contract in OpenAPI, including private-first and cancellation-boundary semantics. Implement D-06 truthful cancellation and D-08 protected durable-status exposure; keep public publication, metadata/hashtags, URL persistence, and retention out of route and documentation scope.</action>
  <verify><automated>npm run typecheck && npm run build</automated></verify>
  <done>Protected lifecycle routes and OpenAPI describe only API-owned private-job operations and expose no server/provider secrets or filesystem internals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the fake-provider lifecycle verifier and close draft lifecycle coverage</name>
  <files>src/scripts/verify-youtube-trailer-job-lifecycle.ts, src/scripts/verify-trailer-video-upload-lifecycle.ts, package.json</files>
  <behavior>
    - Fake provider scenarios cover private session creation, persisted session before bytes, 308 offset resume, accepted private provider ID, distinct processing polling, normalized retryable/terminal failure, OAuth readiness rejection, restart reconciliation, and non-overlapping worker execution.
    - Duplicate start for one current fingerprint returns one active job; replacement obsoletes old work, rejects late lease/source completion, retains accepted private provider evidence, and cancel reports its true local/provider boundary.
    - Direct protected route/OpenAPI assertions prove no credential/session/raw-provider/path values occur in public DTOs, and expanded draft verifier retains every compensation case from Plan 16-01.
  </behavior>
  <action>Create the compiled development verifier with an injected deterministic fake provider and isolated SQLite/media fixtures; invoke the actual exported worker startup/stop seam and direct Express router stack without an HTTP listener or live provider call. Add npm command `verify:youtube-trailer-job-lifecycle` that runs compiled output with development temporary roots and disabled unrelated background workers. Assert D-04 through D-08 race/recovery/cancellation outcomes, private-first transfer, OAuth upload-scope readiness failure, and safe DTO/OpenAPI redaction. Keep the Plan 16-01 verifier's compensation scenarios active and expand only where route integration needs a regression assertion; add no packages or test framework.</action>
  <verify><automated>npm run build && npm run verify:trailer-video-upload-lifecycle && npm run verify:youtube-trailer-job-lifecycle</automated></verify>
  <done>Offline compiled verification exercises the real job orchestration with fake provider responses, proves every Phase 16 lifecycle boundary, and makes no network/provider call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client to lifecycle API | Client can request lifecycle actions but may not supply source paths, credentials, sessions, or provider state. |
| verifier fake provider to worker | Test-only provider simulates external outcomes while proving no live credential/network dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-16-11 | I | status/cancel DTO and OpenAPI | high | mitigate | DTO mapper, route assertions, and OpenAPI verifier exclude provider credentials, session locations, raw failures, and filesystem data. |
| T-16-12 | E | lifecycle endpoints | high | mitigate | `requireAuth`, strict identifiers, episode scoping, and no-store headers protect all start/status/cancel operations. |
| T-16-13 | R/T | fake-provider lifecycle proof | medium | mitigate | Deterministic injected provider and direct worker/router tests cover the persisted acceptance, reconciliation, and lease/source race boundaries. |
| T-16-SC | T | package supply chain | low | accept | No package installation or package manifest mutation is planned; package.json only receives a local verifier command. |
</threat_model>

<verification>
Run `npm run typecheck && npm run build && npm run verify:trailer-video-upload-lifecycle && npm run verify:youtube-trailer-job-lifecycle`. Confirm the verifier uses temporary fixtures and fake-provider injection only.
</verification>

<success_criteria>
All Phase 16 requirements are executable and observable: protected private job DTOs, durable/recoverable source lifecycle, and atomic owner-bound draft promotion are proven without live YouTube calls or Phase 17 public-publish scope.
</success_criteria>

<output>
Create `.planning/phases/16-draft-staging-and-private-youtube-job/16-04-SUMMARY.md` when done.
</output>
