---
phase: 16-draft-staging-and-private-youtube-job
plan: "03"
type: execute
wave: 2
depends_on: [16-01, 16-02]
files_modified:
  - src/config/env.ts
  - .env.example
  - src/services/youtube-trailer-upload.provider.ts
  - src/services/youtube-trailer-job.service.ts
  - src/services/episode-trailer-video.service.ts
  - src/workers/youtube-trailer-job.worker.ts
  - src/server.ts
autonomous: true
requirements: [TRAILER-02, TRAILER-03, TRAILER-09]
user_setup:
  - service: YouTube Data API OAuth
    why: Live private uploads require the deployment refresh-token grant to authorize `youtube.upload`; compiled verification uses a fake provider and requires no credential.
    env_vars:
      - name: YOUTUBE_CLIENT_ID
        source: Google Cloud OAuth client configuration
      - name: YOUTUBE_CLIENT_SECRET
        source: Google Cloud OAuth client configuration
      - name: YOUTUBE_REFRESH_TOKEN
        source: Operator-controlled OAuth authorization containing the upload scope
must_haves:
  truths:
    - A worker starts one non-overlapping durable private transfer, persists the resumable session before bytes, reconciles session/video after restart, and polls provider processing separately from upload.
    - Replacement makes an older source job obsolete and no late worker result can update the current source; accepted private provider media remains available for reconciliation.
    - Missing upload authorization disables/fails a job safely without provider transfer or secret disclosure.
  artifacts:
    - src/services/youtube-trailer-upload.provider.ts
    - src/services/youtube-trailer-job.service.ts
    - src/workers/youtube-trailer-job.worker.ts
  key_links:
    - The worker delegates all durable changes through repository lease/revision transitions from Plan 16-02.
    - Final-video replacement invokes source obsoletion before any old worker completion could affect current state.
---

<objective>
Implement the server-owned private resumable upload, reconciliation, and non-overlapping worker lifecycle.

Purpose: turn durable rows into a truthful private-first YouTube workflow that survives interruption and replacement without browser OAuth or duplicate provider videos.
Output: injectable documented-protocol provider, OAuth readiness check, job orchestration, replacement obsoletion, and startup worker.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-CONTEXT.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-RESEARCH.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-PATTERNS.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-02-SUMMARY.md

<interfaces>
Consume Plan 16-02 repository operations for active-source lookup, claims, guarded updates, retries, cancellation, and obsolete rows. Follow `src/services/youtube-metrics.service.ts` for server-side OAuth refresh/error boundaries and `src/workers/episode-artifact-preparation.worker.ts` for one module-local non-overlapping run plus startup recovery and stop callback. The provider interface must be injectable by Plan 16-04's fake provider.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a private resumable provider adapter and safe OAuth readiness configuration</name>
  <files>src/config/env.ts, .env.example, src/services/youtube-trailer-upload.provider.ts</files>
  <behavior>
    - Readiness obtains server-side OAuth access and reports a normalized recoverable configuration error when credentials or upload scope are absent, without attempting a video transfer.
    - Session initiation sends private status, saves the session location for service persistence, resumes via status/Range semantics, and returns a provider video ID only after provider acceptance.
    - Processing lookup returns normalized provider status/details and no access token, refresh token, session URI, raw response, or local path reaches caller-facing types.
  </behavior>
  <action>Extend the existing YouTube config family with conservative enabled, timeout, bounded chunk, polling, and retry/backoff settings plus documented environment examples; do not add a package or a second credential scheme. Build a narrow injectable adapter using existing `google-auth-library` and Node primitives for OAuth readiness, private resumable session initiation, session-status reconciliation, bounded streaming chunks, and authorized processing lookup. Require an upload-capable grant before any upload session and map absent scope/credential/HTTP failures to safe categories. Per D-06, expose provider acceptance facts rather than claiming remote rollback; retain session/provider references only in internal types.</action>
  <verify><automated>npm run typecheck && npm run build</automated></verify>
  <done>The live adapter has documented private-first resumable protocol methods and a fail-safe upload-scope readiness gate, while a fake adapter can implement the same contract offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orchestrate durable transfer, recovery, cancellation, replacement, and startup work</name>
  <files>src/services/youtube-trailer-job.service.ts, src/services/episode-trailer-video.service.ts, src/workers/youtube-trailer-job.worker.ts, src/server.ts</files>
  <behavior>
    - The service fingerprints a regular canonical final MP4 as SHA-256 plus bytes and relative filename, creates/reuses one active source job, and rechecks evidence before every provider-result write.
    - The worker has one active run, persists intent before provider effects, persists session/video evidence before the next effect, resumes/reconciles after restart, polls private processing, and backs off retryable failures.
    - A cancel request stops future local work and reports acceptance truthfully; a replacement obsoletes old work, requests safe local stop, retains accepted private provider media, and blocks late completion via guarded transitions.
  </behavior>
  <action>Implement the job service and worker on the Plan 16-02 state/repository contract. Derive every source path from the current episode final media and use regular-file evidence; create/reuse only the current fingerprint job, persist each external intent/result in order, and use lease/revision/source predicates for every update. Implement D-04 replacement obsoletion from `replaceEpisodeTrailerVideo`, D-05 reconciliation retention, D-06 truthful cancellation boundary, D-07 same-session/provider retry and recovery when source is unchanged, and D-08 restart-safe state. Start one unref'd non-overlapping worker after database/media initialization, honor `DISABLE_BACKGROUND_WORKERS`, recover only at startup, and expose a stop callback for offline verifier cleanup. Do not create public publishing/update, metadata/hashtag, retention, or browser-side OAuth behavior.</action>
  <verify><automated>npm run typecheck && npm run build</automated></verify>
  <done>A single process can safely recover and progress exactly one private job at a time, while source replacement/cancellation/restart cannot duplicate a provider video or accept stale completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service/worker to YouTube API | OAuth, resumable session URI, and provider state cross to an external provider. |
| canonical final media to worker | A local replacement can change the file behind the same canonical filename during asynchronous transfer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-16-07 | I/E | OAuth and provider adapter | critical | mitigate | Server-only refresh flow, upload-scope readiness, no browser credential/session types, and normalized errors. |
| T-16-08 | T/R | resumable worker writes | high | mitigate | Persist-before-effect ordering plus fingerprint/lease/revision checks and provider reconciliation prevent uncertain retries or late writes. |
| T-16-09 | D | MP4 transfer worker | high | mitigate | One bounded streaming worker, configured backoff, and `DISABLE_BACKGROUND_WORKERS` avoid concurrent memory/transfer exhaustion. |
| T-16-10 | T | replacement/cancellation | high | mitigate | D-04 through D-07 obsolete stale source work, stop only local work safely, and retain accepted private provider media for reconciliation. |
| T-16-SC | T | package supply chain | low | accept | No package installation or package manifest mutation is planned in this plan. |
</threat_model>

<verification>
Run `npm run typecheck && npm run build`. Plan 16-04 proves this adapter/worker against an injected fake provider, never a live channel.
</verification>

<success_criteria>
TRAILER-02 and TRAILER-03 have a private-first, source-fingerprinted, resumable, restart-safe worker lifecycle with honest cancellation and safe OAuth readiness.
</success_criteria>

<output>
Create `.planning/phases/16-draft-staging-and-private-youtube-job/16-03-SUMMARY.md` when done.
</output>
