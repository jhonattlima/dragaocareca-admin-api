---
phase: 18-episode-hashtag-authoring
plan: "02"
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/database/sqlite.ts
  - src/database/repositories/youtube-hashtag-cache.repository.ts
  - src/services/youtube-hashtag-search.service.ts
  - src/services/episode-hashtag-authoring.service.ts
autonomous: true
requirements:
  - TRAILER-06
must_haves:
  truths:
    - "All 50 valid normalized Gemini candidates are looked up cache-first and serially under server-side quota/rate admission, per D-02."
    - "Only Gemini-relevant candidates can enter deterministic top-three ranking, per D-03."
    - "Provider/cache failures resolve to safe recoverable unavailable outcomes without changing transcript or summary completion, per D-04."
  artifacts:
    - path: src/database/repositories/youtube-hashtag-cache.repository.ts
      provides: "Durable normalized YouTube approximate-count cache and daily admission accounting"
    - path: src/services/youtube-hashtag-search.service.ts
      provides: "One-lane cache-first public YouTube count lookup with safe DTOs"
    - path: src/services/episode-hashtag-authoring.service.ts
      provides: "Gemini structured 50-candidate validation, relevance gate, and ranking"
  key_links:
    - from: src/services/episode-hashtag-authoring.service.ts
      to: src/services/youtube-hashtag-search.service.ts
      via: "validated normalized candidates enter serial cache-first lookup"
    - from: src/services/youtube-hashtag-search.service.ts
      to: src/database/repositories/youtube-hashtag-cache.repository.ts
      via: "search shape cache key, expiry, and daily admission ledger"
---

<objective>
Implement the durable cache/admission and provider-service contracts that turn a summary-grounded Gemini candidate set into safe, relevance-gated approximate YouTube counts.

Purpose: Meet the full 50-candidate requirement without exhausting public-search quota or exposing provider implementation details.
Output: SQLite cache persistence, a serial YouTube lookup boundary, and structured Gemini authoring/ranking services.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-episode-hashtag-authoring/18-CONTEXT.md
@.planning/phases/18-episode-hashtag-authoring/18-RESEARCH.md
@.planning/phases/18-episode-hashtag-authoring/18-01-PLAN.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-PATTERNS.md
@src/database/sqlite.ts
@src/database/repositories/youtube-trailer-job.repository.ts
@src/services/youtube-metrics.service.ts
@src/services/episode-summary.service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist normalized count-cache and daily-admission records</name>
  <files>src/database/sqlite.ts, src/database/repositories/youtube-hashtag-cache.repository.ts</files>
  <behavior>
    - Cache identity distinguishes normalized tag, region, language, and search-shape version; a fresh result is reusable across automatic and manual callers.
    - A cache miss consumes exactly one admission from its strict Pacific-day 90 automatic or 10 manual reservation; neither class may borrow the other allocation, while an expired or denied miss produces no provider claim.
    - Migration is idempotent and repository APIs return typed rows instead of raw SQLite data.
  </behavior>
  <action>Add idempotent SQLite schema/migration support for a normalized YouTube hashtag count cache and Pacific-date admission ledger. Cache rows must contain normalized lookup identity, approximate count, retrieval/expiry times, and a safe provider-error category only; indexes must support exact fresh-hit lookup and daily caller-class accounting. Create a focused typed repository that derives no filesystem locations, maps SQLite rows, records successful/zero-result cache entries, reads fresh entries, and atomically admits a request against the fixed 90 automatic or 10 manual allocation for its Pacific quota date. Enforce strict non-borrowing: automatic admissions never read or consume manual capacity and manual admissions never read or consume automatic capacity; cache hits make no ledger mutation. Use a transaction or conditional increment so concurrent requests cannot overspend either bucket. Persist neither OAuth values, raw provider payloads, operator request text, nor provider URLs.</action>
  <verify>
    <automated>npm run build && npm run verify:episode-hashtag-authoring -- --focus=lookup</automated>
  </verify>
  <done>A restart-safe SQLite cache shares normalized results across callers and enforces daily quotas without leaking provider internals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cache-first one-lane YouTube lookup and safe manual DTOs</name>
  <files>src/services/youtube-hashtag-search.service.ts</files>
  <behavior>
    - Equivalent whitespace/case/leading-hash input resolves to one normalized cache key and the canonical lower-case display tag.
    - Fresh cache hits return no provider call; misses use a shared one-at-a-time lane, URLSearchParams, existing server OAuth, and return approximate count metadata.
    - Invalid input, credential/provider errors, rate admission denial, and transient failures return safe categories and retry timing without raw upstream data.
  </behavior>
  <action>Create the search service as the sole automatic/manual count boundary. Export one pure normalizer shared by both callers: apply NFKC, trim/collapse, accept at most one leading hash, reject empty/control/embedded-whitespace tags, case-fold the cache key, and return the deterministic canonical lower-case `displayTag` from that key; do not preserve source input casing in DTOs or persistence. Use repository cache-first lookup; on a miss, serialize every provider call through one process-wide lane, admit the caller only through its strict non-borrowable 90 automatic or 10 manual Pacific-day reservation, obtain the existing server OAuth access token via a narrow injectable adapter, and build `search.list` requests with `URLSearchParams` for the configured BR/pt search shape. Parse only `pageInfo.totalResults` as `approximateCount`, cache success or zero results with their configured TTL, and return retrieval time, region, language, source, and cache status. Normalize error categories to the persisted safe set and return retry timing for the durable lifecycle scheduler; retain raw responses, tokens, and URLs inside the provider adapter.</action>
  <verify>
    <automated>npm run build && npm run verify:episode-hashtag-authoring -- --focus=lookup</automated>
  </verify>
  <done>Automatic and manual callers use the same normalized, cache-first, quota-limited, server-only YouTube count boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generate and rank the exact structured candidate set</name>
  <files>src/services/episode-hashtag-authoring.service.ts</files>
  <behavior>
    - Gemini receives only the completed transcript and saved summary as delimited data and must return exactly 50 candidate records with tag, relevance, and 0–100 score.
    - Local validation rejects malformed, duplicate, unsupported, or fewer-than-50-unique-normalized sets before lookup.
    - All 50 normalized candidates receive lookup attempts; top suggestions contain at most three relevant candidates ordered by approximate count, score, then normalized tag.
  </behavior>
  <action>Create an injectable Gemini structured-output adapter and authoring service. Request a JSON object containing exactly 50 `{ tag, relevant, relevanceScore }` records, constrain the provider response schema, and locally validate it with Zod before a lookup begins. Build prompts from the completed transcript and saved generated summary only, delimit both as untrusted source material, and instruct Gemini to avoid unsupported entities and generic/disconnected tags per D-01. Normalize/deduplicate locally; reject the whole generation for recoverable retry unless it supplies 50 unique valid normalized candidates, because D-02 requires every candidate to be checked. Send every retained candidate through the lookup service, then rank only `relevant === true` records by descending approximate count, descending relevance score, and canonical tag; return up to three safe suggestions. Preserve enough sanitized internal candidates/retrieval evidence to retry and persist state, but keep prompt text and raw provider output out of public DTOs.</action>
  <verify>
    <automated>npm run build && npm run verify:episode-hashtag-authoring -- --focus=lookup</automated>
  </verify>
  <done>One structured Gemini request produces an exact validated 50-candidate set, all candidates pass through lookup, and ranking cannot promote irrelevant high-volume tags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript/summary → Gemini prompt | Episode content is untrusted input to a structured generation provider. |
| service → YouTube search | Server OAuth and externally supplied API responses cross the provider boundary. |
| concurrent callers → quota ledger | Automatic batches and manual checks compete for the small daily search budget. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04 | Tampering | Gemini candidate service | high | mitigate | Delimit untrusted source text, require a constrained schema, locally validate all records, and bind final writes to summary identity in the next plan. |
| T-18-05 | Denial of Service | lookup service and repository | high | mitigate | Use durable fresh cache, one shared lane, atomic daily admission, manual reserve, and delayed retries. |
| T-18-06 | Information Disclosure | OAuth/search provider adapter | high | mitigate | Keep tokens, raw URLs, response bodies, and provider errors internal; expose only safe approximate-count metadata/categories. |
| T-18-07 | Tampering | manual tag normalization | medium | mitigate | Canonicalize and validate once, and encode query values with URLSearchParams. |
| T-18-SC | Tampering | npm installs | low | accept | No package installation is planned. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run build && npm run verify:episode-hashtag-authoring -- --focus=lookup`
</verification>

<success_criteria>
- Cached counts survive restarts and protect the public-search quota.
- A generation only advances with exactly fifty valid normalized candidates.
- The chosen three are relevant first, then high-count, and every provider failure remains recoverable.
</success_criteria>

<output>
Create `.planning/phases/18-episode-hashtag-authoring/18-02-SUMMARY.md` when done
</output>
