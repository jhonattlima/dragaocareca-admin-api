---
phase: 18-episode-hashtag-authoring
plan: "03"
type: execute
wave: 3
depends_on:
  - 18-02
files_modified:
  - src/services/episode-summary.service.ts
  - src/workers/episode-hashtag-authoring.worker.ts
  - src/server.ts
  - src/routes/episodes.routes.ts
  - src/docs/openapi.ts
autonomous: true
requirements:
  - TRAILER-06
must_haves:
  truths:
    - "After summary persistence, tags are durably queued into one serialized authoring lane that startup recovery and due retries resume without making transcript/summary generation fail, per D-01 and D-04."
    - "Protected episode/summary responses recover the persisted suggested-tags snapshot after reload, per D-05."
    - "A protected manual lookup returns the same normalized approximate count/retrieval metadata under server-side cache and rate controls, per D-06 and D-07."
  artifacts:
    - path: src/services/episode-summary.service.ts
      provides: "Post-summary durable authoring enqueue and state/digest stale-write protection"
    - path: src/workers/episode-hashtag-authoring.worker.ts
      provides: "Startup recovery and serialized due-retry execution for persisted suggested-tags work"
    - path: src/routes/episodes.routes.ts
      provides: "Protected no-store snapshot and single-tag lookup routes"
    - path: src/docs/openapi.ts
      provides: "Safe documented authoring and manual-lookup contracts"
  key_links:
    - from: src/services/episode-summary.service.ts
      to: src/services/episode-hashtag-authoring.service.ts
      via: "successful durable summary state queues one tag stage in the serial tail"
    - from: src/routes/episodes.routes.ts
      to: src/services/youtube-hashtag-search.service.ts
      via: "authenticated manual lookup uses the same normalizer/cache/admission/DTO"
---

<objective>
Wire durable serialized authoring/recovery into the established summary lifecycle and expose safe protected contracts for persisted suggestions and a single manual lookup.

Purpose: Make advisory tags reliably reachable by the future admin field while preserving summary success and server-only provider ownership.
Output: Restart-safe sequential integration, authenticated no-store routes, and matching OpenAPI documentation.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-episode-hashtag-authoring/18-CONTEXT.md
@.planning/phases/18-episode-hashtag-authoring/18-RESEARCH.md
@.planning/phases/18-episode-hashtag-authoring/18-02-PLAN.md
@.planning/milestones/v1.2-phases/09-summary-runtime-and-draft-contract/09-02-SUMMARY.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-04-SUMMARY.md
@src/services/episode-summary.service.ts
@src/workers/youtube-trailer-job.worker.ts
@src/server.ts
@src/routes/episodes.routes.ts
@src/docs/openapi.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist and recover serialized tag-authoring work</name>
  <files>src/services/episode-summary.service.ts, src/workers/episode-hashtag-authoring.worker.ts, src/server.ts</files>
  <behavior>
    - A summary reaches done and is written before exactly one persisted pending tag stage enters the shared serialized authoring lane.
    - Startup recovery and due retries process persisted pending, interrupted processing, and due unavailable states through that same lane, never concurrently.
    - Tag provider/admission failure changes only `suggestedTags` to unavailable with safe error/retry data; the summary remains done.
    - A regenerated summary changes root state version/digest and prevents every old processing, retry, or terminal write from overwriting the newer lifecycle.
  </behavior>
  <action>Extend the existing summary service rather than the transcription worker so tags begin only after the summary artifact and `aiSummary: done` state have been durably written per D-01. Persist `suggestedTags.pending` with root state version and SHA-256 saved-summary digest before enqueueing one shared process-wide serialized authoring lane. Add the enabled startup worker and server bootstrap registration: its startup pass finds persisted pending, interrupted processing, and due unavailable states; it converts interrupted processing to the documented retryable state, queues every eligible episode through that same lane, schedules the next due retry with an injectable timer, and returns a stop callback for shutdown/test cleanup. Guard the pending-to-processing transition and every unavailable/done/retry persistence by re-reading state and requiring the captured root version plus summary digest; discard any old processing result or timer callback when either differs, while allowing normalized global cache entries to remain reusable. Isolate Gemini, lookup, quota, and retry failures into `suggestedTags.status = unavailable` with incremented bounded attempt count, safe category, and durable retry time; summary/transcript state remains exactly as written. Expose a sanitized suggested-tags snapshot through the existing protected summary-status contract without reads causing generation. This implements D-01, D-04, and D-05 with restart-safe automatic retry execution.</action>
  <verify>
    <automated>npm run build && npm run verify:episode-hashtag-authoring -- --focus=lifecycle</automated>
  </verify>
  <done>Summary completion durably queues one independent serial tag stage; startup recovery and due retries resume it, and failures/stale work cannot corrupt a completed summary or newer suggestions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add protected no-store snapshot and manual lookup routes</name>
  <files>src/routes/episodes.routes.ts</files>
  <behavior>
    - The protected episode/summary response returns the persisted safe suggested-tags state and at most three suggestions after reload.
    - A single manual tag request normalizes server-side and returns the same approximate count/retrieval DTO used by automatic lookup.
    - Authentication, malformed body/path values, unavailable providers, and quota denial return stable safe JSON with no-store already applied.
  </behavior>
  <action>Add a focused protected endpoint under the existing episode route namespace for one manual hashtag lookup, using `requireAuth` and route-local `Cache-Control: no-store` before authentication. Validate a strict JSON body containing one tag with Zod; call the shared search service rather than duplicating normalization, cache, OAuth, or the strict non-borrowable 10-call manual reservation. Return the canonical lower-case display tag and safe approximate-count DTO for success/cache hit and a sanitized unavailable/retry DTO for admitted-but-unavailable conditions; map invalid tags to 400 and keep access/provider secrets, raw upstream errors, cache ledger details, prompts, transcript, and summary text out of every response. Extend the existing protected episode or summary response mapper to include only the persisted `suggestedTags` snapshot needed for the future field, with no route-side regeneration. This implements D-05, D-06, and the backend side of D-07; the two-second browser debounce remains frontend-owned.</action>
  <verify>
    <automated>npm run build && npm run verify:episode-hashtag-authoring -- --focus=route</automated>
  </verify>
  <done>Authenticated clients can retrieve persisted suggestions and manually inspect one normalized tag through the same bounded backend lookup path.</done>
</task>

<task type="auto">
  <name>Task 3: Document the protected advisory-tag contracts</name>
  <files>src/docs/openapi.ts</files>
  <action>Document the protected persisted suggested-tags shape and manual lookup operation in OpenAPI. Name counts `approximateCount`; include canonical lower-case display/normalized tag, retrieved timestamp, region, language, source, cache status, state, safe error category, and retry time where applicable. Document bearer authentication and no-store response behavior, strict one-tag request grammar, 400/401 outcomes, the approximate/advisory nature of returned top-three suggestions, and that automatic/manual quota allocations are server-managed and not exposed as ledger values. Keep the contract separate from trailer upload, private job, publication, playlist, retention, browser OAuth, and title metadata endpoints, per the Phase 18 boundary and deferred ideas.</action>
  <verify>
    <automated>npm run build && npm run verify:episode-hashtag-authoring -- --focus=route</automated>
  </verify>
  <done>OpenAPI describes only the safe protected authoring snapshot and manual count contract, aligned with route behavior and provider confidentiality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| summary completion → asynchronous authoring | A later summary generation can race external authoring work. |
| client → protected lookup route | A user-controlled tag reaches quota-sensitive provider lookup. |
| service DTO → OpenAPI/client | Only advisory/safe fields may cross the API boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-08 | Tampering | `episode-summary.service.ts` | high | mitigate | Gate authoring after durable summary write and compare root state version plus summary digest before tag persistence. |
| T-18-09 | Denial of Service | manual lookup route | high | mitigate | Apply auth and no-store first, then use the shared normalizer, durable cache, one lane, and manual admission reserve. |
| T-18-10 | Information Disclosure | route/OpenAPI DTO | high | mitigate | Use explicit allowlists for retrieval/error fields; exclude transcripts, prompts, tokens, provider payloads, URLs, and ledger internals. |
| T-18-11 | Repudiation | persisted suggestions | medium | mitigate | Persist state timestamps, prompt version, summary digest, cache status, and safe retrieval metadata. |
| T-18-SC | Tampering | npm installs | low | accept | No package installation is planned. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run build && npm run verify:episode-hashtag-authoring -- --focus=lifecycle`
- `npm run build && npm run verify:episode-hashtag-authoring -- --focus=route`
</verification>

<success_criteria>
- Tags become available after summary completion without coupling to trailer lifecycle work.
- The protected API reuses one persisted snapshot and one lookup control plane.
- API documentation makes count approximation, authentication, retry/unavailable behavior, and safe fields explicit.
</success_criteria>

<output>
Create `.planning/phases/18-episode-hashtag-authoring/18-03-SUMMARY.md` when done
</output>
