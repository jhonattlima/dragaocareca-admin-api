---
phase: 18-episode-hashtag-authoring
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/verify-episode-hashtag-authoring.ts
  - package.json
  - src/config/env.ts
  - .env.example
  - src/schemas/episode-draft-state.ts
autonomous: true
requirements:
  - TRAILER-06
user_setup:
  - service: Gemini API
    why: "Generate the structured 50-candidate advisory set after summary completion."
    env_vars:
      - name: GEMINI_API_KEY
        source: "Existing server secret configuration"
  - service: YouTube Data API OAuth
    why: "Retrieve server-side approximate public search counts using the existing refresh-token boundary."
    env_vars:
      - name: YOUTUBE_CLIENT_ID
        source: "Existing Google Cloud OAuth configuration"
      - name: YOUTUBE_CLIENT_SECRET
        source: "Existing Google Cloud OAuth configuration"
      - name: YOUTUBE_REFRESH_TOKEN
        source: "Existing server secret configuration"
must_haves:
  truths:
    - "A completed summary can carry an independently durable suggested-tags lifecycle without a second episode state file, per D-05."
    - "The implementation has a compiled offline verification foundation before Gemini, OAuth, or YouTube search code is introduced."
    - "Operational limits for cache freshness, one-at-a-time lookup admission, automatic retries, and reserved manual capacity are validated from environment configuration."
  artifacts:
    - path: src/schemas/episode-draft-state.ts
      provides: "Suggested-tags child-state schema and normalization"
    - path: src/config/env.ts
      provides: "Bounded hashtag authoring/cache/quota/retry configuration"
    - path: src/scripts/verify-episode-hashtag-authoring.ts
      provides: "Temporary-fixture, no-network verification scaffold"
  key_links:
    - from: src/config/env.ts
      to: src/services/youtube-hashtag-search.service.ts
      via: "typed authoring settings control cache and admission behavior"
    - from: src/schemas/episode-draft-state.ts
      to: src/services/episode-summary.service.ts
      via: "episode.state.json suggestedTags child persists advisory status"
---

<objective>
Create Phase 18's executable verification foundation and durable configuration/state contract before provider or route work begins.

Purpose: Make the later Gemini and YouTube implementation deterministic, quota-bounded, and testable without live credentials.
Output: A compiled verifier scaffold, typed configuration, documented environment variables, and the `suggestedTags` state child.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-episode-hashtag-authoring/18-CONTEXT.md
@.planning/phases/18-episode-hashtag-authoring/18-RESEARCH.md
@.planning/milestones/v1.2-phases/09-summary-runtime-and-draft-contract/09-02-SUMMARY.md
@.planning/phases/16-draft-staging-and-private-youtube-job/16-05-SUMMARY.md
@src/config/env.ts
@src/schemas/episode-draft-state.ts
@src/scripts/verify-youtube-trailer-job-lifecycle.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish an isolated compiled hashtag-authoring verifier</name>
  <files>src/scripts/verify-episode-hashtag-authoring.ts, package.json</files>
  <action>Create the `verify-episode-hashtag-authoring` compiled verifier and npm command following the Phase 16 direct-service/direct-router pattern. Require development mode; create a unique temporary SQLite/media fixture; assert the fixture is under the temporary directory; force cleanup in `finally`; and provide `--focus=state`, `--focus=lookup`, and `--focus=route` branches for subsequent plans. Design explicit injection seams for fake Gemini candidate generation, fake YouTube count lookup, clock, and rate-admission state, and make the scaffold fail if a live fetch or OAuth-token path is reached. Per D-01 through D-07, keep every planned assertion offline and make the no-network guarantee part of the scaffold contract.</action>
  <verify>
    <automated>npm run build && npm run verify:episode-hashtag-authoring -- --focus=state</automated>
  </verify>
  <done>A build-output verifier command runs from a temporary fixture, supports focused execution, cleans up deterministically, and has no live-provider dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define bounded authoring configuration and persisted suggested-tags state</name>
  <files>src/config/env.ts, .env.example, src/schemas/episode-draft-state.ts</files>
  <behavior>
    - Invalid integer env values for authoring limits fail configuration parsing; omitted values resolve to documented conservative defaults.
    - Episode-state normalization preserves a valid `suggestedTags` child and initializes it for existing state documents without adding a second file.
    - The child represents pending, processing, done, and unavailable outcomes with safe retry/error/retrieval fields.
  </behavior>
  <action>Extend the central config with a dedicated `youtube.hashtagAuthoring` block and mirror every key in `.env.example`: enabled switch; Gemini model/prompt version; cache success and zero-result TTLs; one permitted lookup lane; 90 automatic and 10 manual calls per Pacific-time quota day; BR/pt search shape; bounded request timeout; and 1, 5, 15, 60-minute retry delays. Validate all numeric values with the existing bounded-positive-integer style and enforce that automatic plus manual capacity cannot exceed the documented 100-call daily bucket. Extend `EpisodeDraftState` with a `suggestedTags` child per D-05, including status, state version, summary digest, timestamps, retry time, normalized safe error category, and only sanitized candidate/retrieval/suggestion records. Normalize legacy state into this child as idle and retain the shared root version so later writes can reject stale summary generations. Use the conservative defaults above as agent discretion and document that `pageInfo.totalResults` is approximate.</action>
  <verify>
    <automated>npm run build && npm run verify:episode-hashtag-authoring -- --focus=state</automated>
  </verify>
  <done>Configuration exposes validated cache/quota/retry controls, and every `episode.state.json` has one normalized suggested-tags child with safe recoverable states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator environment → configuration | Limits and provider enablement determine external-call admission. |
| episode state file → authoring workflow | Existing or concurrent state must not create an ambiguous/stale tags lifecycle. |
| verifier fixtures → persistence modules | Offline validation must not touch the configured database, OAuth token, or network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Denial of Service | `src/config/env.ts` | high | mitigate | Bound daily automatic/manual budgets, retry delays, TTLs, and the single lookup lane in typed configuration. |
| T-18-02 | Tampering | `src/schemas/episode-draft-state.ts` | high | mitigate | Normalize one versioned `suggestedTags` child inside the established episode state and require later state/digest compare-and-set writes. |
| T-18-03 | Information Disclosure | verifier fixture | medium | mitigate | Use temporary fixtures and fake seams only; assertions reject live provider/OAuth execution and cleanup runs in `finally`. |
| T-18-SC | Tampering | npm installs | low | accept | No package installation is planned; this phase reuses Node, Zod, SQLite, native fetch, and existing Google OAuth support. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run build && npm run verify:episode-hashtag-authoring -- --focus=state`
</verification>

<success_criteria>
- The phase starts with executable offline validation rather than live Gemini or YouTube calls.
- A summary state can persist advisory tags, retry metadata, and safe errors in the existing shared state document.
- Environment defaults protect the small YouTube search quota and reserve manual capacity.
</success_criteria>

<output>
Create `.planning/phases/18-episode-hashtag-authoring/18-01-SUMMARY.md` when done
</output>
