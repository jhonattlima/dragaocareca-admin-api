---
phase: 12-secure-episode-artifact-downloads
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - src/services/episode-artifact-download.service.ts
  - src/scripts/verify-episode-artifact-downloads.ts
autonomous: true
requirements: [ART-01, ART-02, ART-03, ART-04, ART-05]
must_haves:
  truths:
    - "A fixed catalog translates only the five public selectors into canonical final artifact filenames."
    - "An omitted selector chooses all catalog entries, while malformed, empty, repeated-key, and unknown selector shapes fail before storage access."
    - "Availability preflight admits only regular files resolved by the existing final-media helper and reports catalog-ordered available and missing selectors."
    - "The already-runnable verifier proves the selector and preflight contract after this wave."
  artifacts:
    - path: "src/services/episode-artifact-download.service.ts"
      provides: "Typed selector parser, final-only catalog, and availability preflight"
    - path: "src/scripts/verify-episode-artifact-downloads.ts"
      provides: "Progressively extended selector and preflight contract verifier"
  key_links:
    - from: "public artifact selector"
      to: "getEpisodeMediaFinalPath"
      via: "closed typed catalog"
    - from: "preflight result"
      to: "compiled verifier"
      via: "deterministic temporary final-media fixtures"
---

<objective>
Implement and prove the typed final-artifact selector and availability-preflight boundary.

Purpose: All request-derived values must become closed catalog entries before any filesystem call, preventing path disclosure and making final-only selection executable against the verifier scaffold created in Plan 12-02.
Output: Reusable parser/preflight service plus verifier assertions that run after this security-critical implementation wave.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-secure-episode-artifact-downloads/12-CONTEXT.md
@.planning/phases/12-secure-episode-artifact-downloads/12-RESEARCH.md
@.planning/phases/12-secure-episode-artifact-downloads/12-PATTERNS.md
@.planning/phases/12-secure-episode-artifact-downloads/12-VALIDATION.md

<interfaces>
From `src/services/episode-media-layout.service.ts`:
`export type EpisodeMediaKind = "audio" | "trailer" | "cover" | "coverLow" | "transcript";`
`export const getEpisodeMediaFinalPath = (episodeId: number, kind: EpisodeMediaKind): string`.

The new service may call only `getEpisodeMediaFinalPath` for artifact resolution. Extend the Plan 12-02 verifier scaffold rather than creating another verification entry point.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the closed selector catalog and final-file availability preflight</name>
  <read_first>`src/services/episode-media-layout.service.ts`, `.planning/phases/12-secure-episode-artifact-downloads/12-CONTEXT.md`, `.planning/phases/12-secure-episode-artifact-downloads/12-RESEARCH.md` (Patterns 1 and 2), `.planning/phases/12-secure-episode-artifact-downloads/12-PATTERNS.md`</read_first>
  <files>src/services/episode-artifact-download.service.ts</files>
  <behavior>
    - Omitted `artifacts` selects catalog-order `episode`, `trailer`, `transcript`, `image`, and `image-low`.
    - A single valid CSV selects only known values, removes duplicate values within that CSV, and returns selected entries in catalog order.
    - Array/repeated-key query values, non-string input, an empty value or segment, and unknown names produce a typed validation failure for the route to convert to `400`.
    - Preflight resolves each selection only through `getEpisodeMediaFinalPath`, accepts regular files, returns deterministic archive entry names under `episode-<episodeId>/`, and separates missing selector names without returning paths to callers that log or respond.
  </behavior>
  <action>Create a small function-based service exporting the five-selector catalog, a strict query parser, and an asynchronous preflight result type. Implement D-05 and D-09 exactly: absent query means all five; a supplied query must be one nonempty string CSV with no empty segments or unknown values; normalize valid output into fixed catalog order. Map selectors to the existing `EpisodeMediaKind` and canonical final filenames per D-04. For each selection, call only `getEpisodeMediaFinalPath(episodeId, kind)` then `lstat` or `stat`; admit regular files only. Return archive entry names as `episode-<episodeId>/<canonical filename>` per D-02, plus requested, available, and missing selector lists for D-08 and D-12. Keep absolute paths internal to the entry object consumed solely by the later route; no request field, stored filename, directory scan, glob, legacy resolver, staging, backup, draft, state, or summary artifact participates in resolution, per D-11.</action>
  <verify><automated>npm run typecheck &amp;&amp; npm run build</automated></verify>
  <done>Acceptance criteria: the service provides typed parsing and preflight APIs that preserve catalog order, distinguish invalid input from missing final files, and can authorize only known canonical final regular files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend and run the verifier for selector and preflight behavior</name>
  <read_first>`src/scripts/verify-episode-artifact-downloads.ts`, `src/services/episode-artifact-download.service.ts`, `src/services/episode-media-layout.service.ts`, `.planning/phases/12-secure-episode-artifact-downloads/12-VALIDATION.md`</read_first>
  <files>src/scripts/verify-episode-artifact-downloads.ts</files>
  <behavior>
    - The compiled verifier proves omitted selection, valid catalog-ordered selection, and rejection of empty, repeated, malformed, and unknown selector inputs.
    - Deterministic temporary media fixtures prove preflight accepts only regular canonical final files, returns canonical prefixed entry names, and reports missing selectors without reporting paths.
  </behavior>
  <action>Replace the Plan 12-02 readiness-only behavior with direct imports and assertions against the new service. Create and remove deterministic temporary final-media fixtures through the canonical helper, and assert D-02, D-04, D-05, D-08, D-09, and D-11 selector/preflight behavior without starting a listener. Keep absolute fixture paths internal to verifier assertions and never write them to output. Run the compiled verifier after adding these assertions so Plan 12-04 depends on a green validation result, per the validation contract.</action>
  <verify><automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run verify:episode-artifact-downloads</automated></verify>
  <done>Acceptance criteria: the pre-existing verifier command is extended and green for the selector and final-file preflight contract before route implementation begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client query → selector parser | Untrusted query values determine which catalog entries may be considered. |
| catalog → local storage | Approved selector entries resolve to server-side paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information Disclosure / Tampering | artifact parser and preflight service | high | mitigate | Fixed typed catalog, singular strict CSV parser, final-path helper only, regular-file preflight, and compiled verifier assertions prevent traversal and non-final artifact inclusion. |
| T-12-03 | Denial of Service | malformed selector input | medium | mitigate | Reject invalid query shapes before filesystem reads and avoid directory enumeration. |
| T-12-SC | Tampering | npm dependency install | high | mitigate | Plan 12-01 approval gates the exact package installation; generated lockfile records the reviewed dependency graph. |
</threat_model>

<verification>
Run `npm run typecheck && npm run build && npm run verify:episode-artifact-downloads` after this wave. Confirm the service references only `getEpisodeMediaFinalPath`. Assumption-delta result: no-change — this phase adds no new primary identity model. External API coverage matrix: not applicable; no external API is integrated.
</verification>

<success_criteria>
The service and its compiled verifier deterministically turn the allowed selector contract into final-only regular-file archive candidates without exposing or accepting a filesystem path.
</success_criteria>

<output>
Create `.planning/phases/12-secure-episode-artifact-downloads/12-03-SUMMARY.md` when done.
</output>
