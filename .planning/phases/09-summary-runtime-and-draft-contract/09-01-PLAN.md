---
phase: 09-summary-runtime-and-draft-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/env.ts
  - .env.example
  - src/services/episode-media-layout.service.ts
  - src/schemas/episode-draft-state.ts
autonomous: true
requirements:
  - OPS-01
  - FLOW-02
must_haves:
  truths:
    - The backend exposes a concrete env-driven summary runtime contract instead of hardcoded command or model choices per D-01, D-11, D-12, and D-13.
    - Episode draft workflow state is generalized into a shared `episode.state.json` contract with separate transcript and summary step objects per D-05 and D-06.
    - Transcript and summary artifacts stay separated, with staging-first ownership during generation so `transcript.txt` remains the sole source input and `summary.txt` remains the editable generated output before later promotion or sync per D-02, D-04, D-05, and D-08.
  artifacts:
    - src/config/env.ts
    - .env.example
    - src/services/episode-media-layout.service.ts
    - src/schemas/episode-draft-state.ts
  key_links:
    - `config.summary` -> summary runtime service arguments
    - staging `episode.state.json` schema -> media-layout helper exports and promoted-path resolvers -> transcription and summary services
    - Legacy `transcript.state.json` fallback -> forward writes to canonical `episode.state.json`
---

<objective>
Establish the summary runtime contract and episode-level draft artifact boundary that later implementation can build on without revisiting the locked decisions.

Purpose: Lock the env surface, artifact paths, and shared state model so Phase 10 can add protected workflow APIs on top of stable backend-owned primitives per D-01 through D-08.
Output: A new summary config block, documented summary env keys, shared episode draft state schema, and media-layout helpers for `summary.txt` and `episode.state.json`.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-summary-runtime-and-draft-contract/09-CONTEXT.md
@.planning/phases/09-summary-runtime-and-draft-contract/09-RESEARCH.md
@.planning/phases/09-summary-runtime-and-draft-contract/09-PATTERNS.md
@.planning/phases/09-summary-runtime-and-draft-contract/09-AI-SPEC.md
@.planning/phases/09-summary-runtime-and-draft-contract/09-COVERAGE.md
@docs/SDD.md
@docs/features/003-episode-transcription/README.md
@docs/features/004-episode-summary-suggestion/PLAN.md
@src/config/env.ts
@src/services/episode-media-layout.service.ts
@src/services/episode-transcription.service.ts
</context>

<flagged_assumptions>
## Flagged assumptions from specless probes

- `A-09-SUMM-01`: Phase 09 satisfies `SUMM-01` by defining service-level transcript-only runtime primitives and draft contracts; protected trigger/read endpoints remain Phase 10 work and must not be backfilled into this plan set.
- `A-09-FLOW-02`: If summary generation is interrupted or queued twice, versioned state in `episode.state.json` is the source of truth and only the current version may write terminal artifacts; later queues supersede stale runs.
- `A-09-OPS-01`: Configuration-driven runtime selection in this phase means env-driven command/model/prompt contracts plus operator-visible example keys and coverage matrix, not automatic binary installation or hosted-provider support.
</flagged_assumptions>

<identity_model_decision>
## Assumption-Delta Architecture Checkpoint

This phase deliberately generalizes draft workflow identity from a transcript-only state file to an episode-level state document. `episode.state.json` becomes the canonical workflow state artifact and `transcript.state.json` becomes legacy read-only fallback during migration per D-05 and D-06.
</identity_model_decision>

<source_audit>
## Multi-Source Coverage Audit

- GOAL: The config-driven runtime and transcript-driven draft contract are covered here via env, state, and artifact boundaries; Plan `09-02` completes the executable summary runtime.
- REQ `OPS-01`: Covered by Task 1 through the new `config.summary` contract and `.env.example` surface.
- REQ `FLOW-02`: Covered by Task 2 through transcript/state helper boundaries that support transcript-ready gating before summary execution.
- RESEARCH: `summary.txt`, `episode.state.json`, no new npm packages, env-driven runtime settings, and backward-compatible state migration are all planned here.
- CONTEXT: D-01 through D-08 are implemented in this plan; D-09 through D-13 are completed by Plan `09-02` prompt/runtime behavior. Deferred frontend UX and transcription-engine replacement are intentionally excluded.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add the env-driven summary runtime contract</name>
  <read_first>@docs/SDD.md, @.planning/phases/09-summary-runtime-and-draft-contract/09-CONTEXT.md, @.planning/phases/09-summary-runtime-and-draft-contract/09-RESEARCH.md, @src/config/env.ts, @.env.example</read_first>
  <files>src/config/env.ts, .env.example</files>
  <acceptance_criteria>
    - `config.summary` exists as a sibling config block with env-driven keys for enablement, runtime command, model path, context size, token cap, timeout, and prompt version per D-01 and OPS-01.
    - `.env.example` documents the `EPISODE_SUMMARY_*` contract with conservative VPS-friendly defaults and leaves the existing transcription env contract unchanged per D-02.
    - Summary runtime settings are centralized in config and are not introduced as ad hoc `process.env` reads in route or service code.
  </acceptance_criteria>
  <action>Update `src/config/env.ts` to add a sibling `summary` block beside `transcription`, with exact identifiers such as `enabled`, `command`, `modelPath`, `contextSize`, `maxTokens`, `timeoutMs`, and `promptVersion`, plus only the low-variance generation controls that the runtime adapter needs per D-01, D-11, D-12, and D-13. Mirror the same keys in `.env.example` using `EPISODE_SUMMARY_ENABLED`, `EPISODE_SUMMARY_COMMAND`, `EPISODE_SUMMARY_MODEL_PATH`, `EPISODE_SUMMARY_CONTEXT_SIZE`, `EPISODE_SUMMARY_MAX_TOKENS`, `EPISODE_SUMMARY_TIMEOUT_MS`, and `EPISODE_SUMMARY_PROMPT_VERSION`. Do not rename, remove, or reuse any `EPISODE_TRANSCRIPTION_*` variables per D-02.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The repo exposes a concrete summary runtime env contract that an operator can configure without changing service code.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize episode draft state and artifact path helpers</name>
  <read_first>@docs/SDD.md, @.planning/phases/09-summary-runtime-and-draft-contract/09-CONTEXT.md, @.planning/phases/09-summary-runtime-and-draft-contract/09-AI-SPEC.md, @src/services/episode-media-layout.service.ts, @src/services/episode-transcription.service.ts, @src/schemas/episode.ts</read_first>
  <files>src/services/episode-media-layout.service.ts, src/schemas/episode-draft-state.ts</files>
  <acceptance_criteria>
    - A shared schema/type contract exists for `episode.state.json` with sibling `transcript` and `aiSummary` step objects per D-05 and D-06.
    - Media-layout helpers expose canonical staging write paths for `summary.txt` and `episode.state.json`, plus legacy fallback and promoted-read helpers for `transcript.state.json`, while keeping `transcript.txt` untouched per D-04 and D-08.
    - The helper contract is sufficient for later services to decide transcript readiness without touching SQLite `episodes.summary`, satisfying FLOW-02 and D-07 through D-10.
  </acceptance_criteria>
  <action>Create `src/schemas/episode-draft-state.ts` as the shared workflow-state contract. Define explicit step-status identifiers for `transcript` and `aiSummary`, plus `version`, timestamps, progress, prompt-version, file-name, and error fields needed for stale-write protection per D-03, D-05, and D-06. Extend `src/services/episode-media-layout.service.ts` with exact helpers such as `getEpisodeMediaDraftSummaryPath`, `getEpisodeMediaDraftStatePath`, `getEpisodeMediaLegacyDraftTranscriptionStatePath`, and focused transcript or summary lookup helpers that make the staging directory the canonical write target during generation while allowing later reads from promoted final paths after save or sync. Preserve backward-compatible reads from the legacy transcript-specific state file, but make `episode.state.json` the canonical write target to record the assumption-delta identity-model shift and lock the staging-first ownership behavior per D-04, D-05, and D-08.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The backend owns a stable shared draft-state and artifact-path contract that later summary and transcription code can consume without guessing filenames.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- File `src/schemas/episode-draft-state.ts`
- Symbol `config.summary`
- Symbols `EpisodeDraftState`, `EpisodeDraftStepStatus`, `TranscriptDraftState`, `AiSummaryDraftState`
- Symbols `getEpisodeMediaDraftSummaryPath`, `getEpisodeMediaDraftStatePath`, `getEpisodeMediaLegacyDraftTranscriptionStatePath`
- Artifact path `summary.txt`
- Artifact path `episode.state.json`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env -> `src/config/env.ts` | Untrusted env values select runtime command, model path, and prompt contract |
| service helpers -> local filesystem | Draft-state and artifact paths must not drift or overwrite transcript/final-summary ownership |
| legacy transcript state -> canonical episode state | Migration must not lose step separation or allow ambiguous write targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | `src/config/env.ts` | high | mitigate | Centralize summary runtime configuration in `config.summary`, validate required command/model fields before execution, and keep prompt/runtime selection operator-driven per D-01 and OPS-01. |
| T-09-02 | Tampering | `src/services/episode-media-layout.service.ts` | high | mitigate | Add dedicated summary/state path helpers so transcript, summary, and shared-state artifacts cannot collapse onto the same filename per D-04, D-05, and D-08. |
| T-09-03 | Information Disclosure | `src/schemas/episode-draft-state.ts` | medium | mitigate | Keep operational state limited to workflow metadata, file names, prompt version, and errors; do not mirror final episode-summary content into SQLite or unrelated files per D-07 and D-10. |
| T-09-SC | Tampering | npm installs | low | accept | No package installation is planned for Phase 09; reuse the existing dependency set only. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run build`
</verification>

<success_criteria>
- Operators can see and configure the summary runtime contract entirely through backend env keys.
- The repo has one canonical episode-level draft state contract that separates transcript source state from summary draft state.
- Phase 10 can build protected summary endpoints without renegotiating filenames, state layout, or draft/final ownership boundaries.
</success_criteria>

<output>
Create `.planning/phases/09-summary-runtime-and-draft-contract/09-01-SUMMARY.md` when done
</output>
