---
phase: 15-final-trailer-video-artifact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/database/sqlite.ts
  - src/database/repositories/episode.repository.ts
  - src/schemas/episode.ts
  - src/services/episode-media-layout.service.ts
autonomous: true
requirements: [TRAILER-01]
must_haves:
  truths:
    - "A protected episode record distinguishes the final trailer video from the existing audio trailer and reports whether it needs a later manual YouTube sync."
    - "Every episode has exactly one server-derived canonical final trailer-video path ending in trailer.mp4."
  artifacts:
    - path: src/database/sqlite.ts
      provides: "Backward-compatible episode columns for final trailer-video metadata and manual-sync state"
    - path: src/services/episode-media-layout.service.ts
      provides: "Canonical final and staging paths for the trailer-video media kind"
    - path: src/database/repositories/episode.repository.ts
      provides: "Typed persistence and protected response mapping for trailer-video metadata"
  key_links:
    - from: src/services/episode-media-layout.service.ts
      to: src/database/repositories/episode.repository.ts
      via: "the persisted final filename is the server-derived relative path for the new media kind"
      pattern: "trailerVideo"
---

<objective>
Create the durable, typed final-trailer-video contract that later upload, ZIP, and YouTube workflows share.

Purpose: Preserve the existing audio trailer while establishing a canonical MP4 artifact and an explicit later-manual-sync state per D-02 and D-04.
Output: SQLite migration guard, episode/repository types, and canonical media-layout helpers.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-final-trailer-video-artifact/15-CONTEXT.md
@src/database/sqlite.ts
@src/database/repositories/episode.repository.ts
@src/schemas/episode.ts
@src/services/episode-media-layout.service.ts

<interfaces>
From `src/services/episode-media-layout.service.ts`:
- `EpisodeMediaKind` is the closed union used by `getEpisodeMediaRelativePath`, `getEpisodeMediaFinalPath`, and staging/backup helpers.
- `episodeMediaFieldForKind` maps persisted media kinds to the repository’s typed media fields.

From `src/database/repositories/episode.repository.ts`:
- `EpisodeRow` is the protected episode response shape.
- `episodeRepository.updateMedia(episodeId, patch)` is the focused persisted media-field update seam.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backward-compatible final trailer-video metadata to episode persistence</name>
  <files>src/database/sqlite.ts, src/database/repositories/episode.repository.ts, src/schemas/episode.ts</files>
  <behavior>
    - Existing databases gain nullable final trailer-video filename and explicit sync-status columns without losing existing episode rows.
    - Episode create/read/update mappings expose trailer-video metadata only through typed, protected episode fields.
    - A video that has never been published reports an unpublished state; a later replacement can be represented as requiring manual synchronization.
  </behavior>
  <action>Add guarded `PRAGMA table_info(episodes)` migrations, SQLite schema columns, schema fields, `SqliteEpisodeRow` mapping, insert/update bindings, and focused repository patch support for final trailer-video filename plus a closed publication-sync status. Use a distinct field family from existing `trailerFileName`, because that field remains the audio trailer. Select a default that accurately represents no YouTube publication yet, and make the status extensible for Phase 16’s successful publication transition. Implement D-04 without adding any YouTube network call or retention behavior assigned to Phase 16.</action>
  <verify><automated>npm run typecheck &amp;&amp; npm run build</automated></verify>
  <done>Fresh and pre-existing SQLite databases load with the new columns, and protected `EpisodeRow` values can carry a final trailer-video filename plus a manual-sync/unpublished state without changing the audio-trailer field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the canonical final trailer-video media kind and paths</name>
  <files>src/services/episode-media-layout.service.ts, src/database/repositories/episode.repository.ts</files>
  <behavior>
    - For episode 42, the relative path is `episodes/42/trailer.mp4` and the final path resolves below the configured episode directory.
    - The trailer-video kind cannot reuse the existing audio-trailer filename or filename field.
    - Staging paths remain server-derived from the episode id and media kind.
  </behavior>
  <action>Extend the closed media-kind contract with a separately named trailer-video kind; map it to the canonical final filename `trailer.mp4` and its new repository media field per D-02. Update helper signatures, migration-layout media loops, and typed record unions so all callers compile and only server-derived relative/final/staging locations are available. Preserve every existing audio trailer path and legacy lookup behavior unchanged.</action>
  <verify><automated>npm run typecheck &amp;&amp; npm run build</automated></verify>
  <done>`getEpisodeMediaRelativePath`, `getEpisodeMediaFinalPath`, and staging promotion can address `episodes/{id}/trailer.mp4`, while the existing audio trailer continues to resolve to `trailer.mp3`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite/media metadata → protected API | Persisted names and sync state become administrator-visible response data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Tampering | episode media metadata | medium | mitigate | Use a closed media-kind/field mapping and repository-owned updates so user input cannot supply a path or overwrite audio trailer metadata. |
| T-15-02 | Repudiation | publication sync status | low | accept | The existing episode `updated_at` timestamp records the mutation; Phase 16 adds publication outcome handling. |
| T-15-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation is authorized by this plan. |
</threat_model>

<verification>
Run TypeScript validation and a production compile after the database, repository, schema, and media-kind signatures converge.
</verification>

<success_criteria>
- The canonical final trailer-video filename is `trailer.mp4`.
- The existing audio trailer remains distinct.
- Protected episode data has a durable state for later manual YouTube synchronization.
</success_criteria>

<output>
Create `.planning/phases/15-final-trailer-video-artifact/15-01-SUMMARY.md` when done.
</output>
