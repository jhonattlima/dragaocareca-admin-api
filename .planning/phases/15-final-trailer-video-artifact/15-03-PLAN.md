---
phase: 15-final-trailer-video-artifact
plan: 03
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/services/episode-artifact-download.service.ts
  - src/scripts/verify-episode-artifact-downloads.ts
autonomous: true
requirements: [TRAILER-05]
must_haves:
  truths:
    - "An authenticated ZIP preparation request can select the final trailer video with a fixed selector."
    - "The requested video is archived only when the canonical final trailer.mp4 is a regular file."
    - "The ZIP entry is episode-{id}/trailer.mp4 and no user-supplied name or path influences it."
  artifacts:
    - path: src/services/episode-artifact-download.service.ts
      provides: "Closed trailer-video artifact selector mapped to the canonical video kind and archive entry"
    - path: src/scripts/verify-episode-artifact-downloads.ts
      provides: "Compiled ZIP selector and source-evidence regression coverage for final trailer video"
  key_links:
    - from: src/services/episode-artifact-download.service.ts
      to: src/services/episode-media-layout.service.ts
      via: "preflight and evidence collection resolve trailer-video only through getEpisodeMediaFinalPath"
      pattern: "trailer-video"
    - from: src/scripts/verify-episode-artifact-downloads.ts
      to: src/routes/episodes.routes.ts
      via: "the existing protected artifact job route exercises the selector and resulting ZIP entries"
      pattern: "artifacts/jobs"
---

<objective>
Extend the closed artifact-download boundary so final trailer videos can be selected and archived safely.

Purpose: Make the newly maintained final MP4 usable through the existing authenticated ZIP lifecycle without creating a general file-download surface, per D-03.
Output: Updated catalog/preflight behavior and compiled artifact-job regression coverage.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-final-trailer-video-artifact/15-CONTEXT.md
@.planning/phases/15-final-trailer-video-artifact/15-01-SUMMARY.md
@.planning/milestones/v1.3-phases/14-reconcile-artifact-job-contract-and-harden-zip-lifecycle/14-01-SUMMARY.md
@src/services/episode-artifact-download.service.ts
@src/services/episode-artifact-preparation.service.ts
@src/scripts/verify-episode-artifact-downloads.ts

<interfaces>
From `src/services/episode-artifact-download.service.ts`:
- `episodeArtifactCatalog()` is the sole selector catalog.
- `parseEpisodeArtifactSelectors()` normalizes known selectors to catalog order.
- `preflightEpisodeArtifactDownloads()` and `collectEpisodeArtifactSourceEvidence()` use `lstat` and `getEpisodeMediaFinalPath` before ZIP preparation/revalidation.

From Phase 15 Plan 01:
- The trailer-video media kind resolves to final `trailer.mp4` under the episode directory.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add trailer-video to the fixed artifact catalog</name>
  <files>src/services/episode-artifact-download.service.ts</files>
  <behavior>
    - `trailer-video` is accepted alongside the existing fixed selectors and normalizes in the catalog’s deterministic order.
    - Its media kind is the final trailer-video kind and its filename is `trailer.mp4`.
    - Preflight treats a missing, symlinked, or directory target as unavailable and never follows a client path.
    - A selected regular final video receives archive entry `episode-{id}/trailer.mp4`.
  </behavior>
  <action>Add one D-03 catalog entry for selector `trailer-video`, the Plan 15-01 trailer-video kind, and canonical filename `trailer.mp4`. Keep strict parsing and final-only `lstat` preflight unchanged: the new selector must inherit rejection of unknown values, arrays, empty selectors, symlinks, directories, staging files, retained histories, and arbitrary paths. Do not rename or reinterpret the existing audio `trailer` selector.</action>
  <verify><automated>npm run typecheck &amp;&amp; npm run build</automated></verify>
  <done>The closed catalog exposes a separate `trailer-video` selector that can produce only `episode-{id}/trailer.mp4` from a regular canonical final file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend compiled ZIP verification for final trailer video</name>
  <files>src/scripts/verify-episode-artifact-downloads.ts</files>
  <behavior>
    - The verifier creates only the canonical final MP4 fixture through media-layout helpers.
    - A protected artifact job selecting trailer-video produces an archive with the exact canonical entry name.
    - Default-all behavior includes the new selector only when that final file is present and reports it as missing otherwise.
    - Selector evidence invalidates a ready archive if the final trailer video changes or appears after a missing-marker archive.
  </behavior>
  <action>Extend the existing compiled offline artifact verifier rather than adding network-based tests. Update its expected selector catalog, fixture/reset helpers, direct protected job-route invocations, OpenAPI-parity expectations that belong to the catalog contract, ZIP central-directory assertions, and source-evidence mutation cases to cover D-03. Assert archive entry names only; do not log or return underlying paths. Retain the existing verification for the audio `trailer` selector so the two artifacts are proven distinct.</action>
  <verify><automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run verify:episode-artifact-downloads</automated></verify>
  <done>The repository-native artifact verifier proves selector parsing, missing handling, ZIP entry naming, and evidence invalidation for final trailer video without widening the artifact allowlist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated artifact selector → ZIP preparation | A requested selector controls which server-owned final artifact may enter an archive. |
| final media file → archive entry | The archive builder converts a canonical regular file into a downloadable ZIP member. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-07 | Elevation of privilege | artifact selector catalog | high | mitigate | Add only D-03 to the closed catalog; preserve parser rejection of filenames, paths, unknown values, and repeated query arrays. |
| T-15-08 | Tampering | ZIP source preflight | high | mitigate | Resolve the trailer-video kind through the canonical final-path helper and retain `lstat` regular-file checks plus source-evidence revalidation. |
| T-15-09 | Information disclosure | archive member names | medium | mitigate | Emit only the fixed `episode-{id}/trailer.mp4` name and preserve existing safe response/logging boundaries. |
| T-15-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation is authorized by this plan. |
</threat_model>

<verification>
Run the existing compiled artifact lifecycle verifier after TypeScript and build validation. It is the regression gate for final-only selector, entry-name, and evidence behavior.
</verification>

<success_criteria>
- `trailer-video` selects only canonical final video.
- ZIPs contain `episode-{id}/trailer.mp4` when the video is available.
- Existing audio trailer selection remains unchanged.
- No staging, history, symlink, directory, filename, or path becomes selectable.
</success_criteria>

<output>
Create `.planning/phases/15-final-trailer-video-artifact/15-03-SUMMARY.md` when done.
</output>
