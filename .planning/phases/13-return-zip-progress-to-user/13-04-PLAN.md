---
phase: 13-return-zip-progress-to-user
plan: 04
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/services/episode-artifact-preparation.service.ts
  - src/scripts/verify-episode-artifact-downloads.ts
autonomous: true
gap_closure: true
requirements: [ZIP-01, ZIP-04, ZIP-07]
must_haves:
  truths:
    - "After a ready manifest becomes stale or expired, identical normalized preparation requests reuse the active queued or preparing job instead of adding duplicate FIFO work."
    - "A valid ready manifest remains an immediate cache hit, while stale and expired manifests cannot shadow a later active manifest with the same cache key."
    - "The compiled verifier proves the invalidation-then-retry regression through the real preparation service."
  artifacts:
    - path: src/services/episode-artifact-preparation.service.ts
      provides: Cache-key candidate selection that prefers active and valid-ready preparation jobs over stale history.
    - path: src/scripts/verify-episode-artifact-downloads.ts
      provides: Regression coverage for repeated normalized prepare calls after cache invalidation.
  key_links:
    - from: prepareEpisodeArtifactArchive
      to: persisted preparation manifests
      via: cache-key candidate filtering and ready-manifest revalidation
      pattern: "queued|preparing|ready"
---

<objective>
Close the stale-manifest cache-key lookup defect so preparation retries stay idempotent after an archive is invalidated or expires.

Purpose: prevent duplicate global FIFO work while preserving the Phase 12 normalized selector contract and Phase 13 cache-validation guarantees.
Output: corrected active-job lookup plus a compiled regression proof.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-return-zip-progress-to-user/13-CONTEXT.md
@.planning/phases/13-return-zip-progress-to-user/13-VERIFICATION.md
@.planning/phases/13-return-zip-progress-to-user/13-01-SUMMARY.md
@src/services/episode-artifact-preparation.service.ts
@src/scripts/verify-episode-artifact-downloads.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the invalidated-cache idempotency regression</name>
  <files>src/scripts/verify-episode-artifact-downloads.ts</files>
  <behavior>
    - Prepare a normalized selector set to ready, mutate a selected final source so status invalidates that manifest, then issue the same normalized prepare request twice.
    - Both retries return the same queued or preparing job ID; the old invalidated manifest is not returned as ready and no second active job is created for that cache key.
    - Exercise selector order normalization with a representative multi-selector CSV so the regression remains tied to the Phase 12 parser contract.
  </behavior>
  <action>Extend the compiled lifecycle verifier with a failing service-level regression adjacent to the existing source-change invalidation assertions. Use real canonical final-media fixtures, the actual selector parser, and preparation service calls; inspect statuses rather than hand-editing manifests for the behavior under test. Assert that cache invalidation followed by repeated identical normalized requests produces one active job ID and preserves the expired prior job only as history. This implements D-05 and D-09.</action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:episode-artifact-downloads</automated>
  </verify>
  <done>The new regression fails against stale-first candidate lookup and demonstrates the required idempotent active-job reuse contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Select reusable active manifests across cache-key history</name>
  <files>src/services/episode-artifact-preparation.service.ts</files>
  <behavior>
    - Queued and preparing manifests for a cache key are reused even when an older manifest for that key is expired or invalidated.
    - Revalidation considers every ready candidate needed to find a currently valid cache hit, but does not resurrect stale archives.
    - When no active or valid-ready candidate exists, exactly one new queued manifest is created for the normalized cache key.
  </behavior>
  <action>Replace the single earliest-manifest lookup in prepareEpisodeArtifactArchive with deterministic cache-key candidate handling. Revalidate ready candidates, then return the current queued or preparing manifest first, otherwise a valid ready manifest as the cache hit; stale, expired, failed, and invalidated history must not block a newer active job. Preserve manifest persistence, selector-only logging, FIFO ordering, SHA-256-or-missing evidence, and all final-only preflight behavior. Do not introduce dependencies or request-derived paths. This implements D-05, D-08, and D-09.</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run verify:episode-artifact-downloads</automated>
  </verify>
  <done>Repeated prepare calls after stale/expired cache history reuse one active or validated-ready job, and the compiled regression is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| protected prepare request → persisted manifest lookup | Authenticated selector input maps to server-owned cache identity and FIFO work. |
| final artifact evidence → ready-cache reuse | Filesystem evidence controls whether an existing archive may be reused. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-08 | DoS | prepareEpisodeArtifactArchive cache-key lookup | high | mitigate | Prefer current queued/preparing and valid-ready candidates so stale history cannot multiply serialized ZIP jobs. |
| T-13-09 | Tampering | ready-manifest evidence | high | mitigate | Revalidate every ready candidate through existing SHA-256-or-missing evidence before returning a cache hit. |
| T-13-SC | Tampering | package supply chain | low | accept | No package installation or dependency change is in this closure plan. |
</threat_model>

<verification>
Run the focused compiled verifier after the RED-to-GREEN change, then run typecheck. The regression must mutate a real selected canonical file and prove two identical retries share one job ID after stale-history invalidation.
</verification>

<success_criteria>
ZIP-01 idempotency holds across expired and invalidated manifest history without weakening ZIP-04 evidence revalidation or creating duplicate FIFO work.
</success_criteria>

## Artifacts

- `src/services/episode-artifact-preparation.service.ts` — corrected cache-key candidate selection.
- `src/scripts/verify-episode-artifact-downloads.ts` — invalidation/retry idempotency regression.

<output>
Create `.planning/phases/13-return-zip-progress-to-user/13-04-SUMMARY.md` when done.
</output>
