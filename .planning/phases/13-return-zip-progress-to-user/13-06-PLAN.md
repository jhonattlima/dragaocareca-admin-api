---
phase: 13-return-zip-progress-to-user
plan: 06
type: execute
wave: 4
depends_on: [13-04, 13-05]
files_modified:
  - src/scripts/verify-episode-artifact-downloads.ts
autonomous: true
gap_closure: true
requirements: [ZIP-02, ZIP-03, ZIP-06, ZIP-07]
must_haves:
  truths:
    - "Compiled verification observes a real queued-to-preparing Archiver run through protected status responses, with source-byte progress reaching an integer 1-99 or increasing while preparing before ready reports 100 only after publication."
    - "Compiled verification starts the actual artifact-preparation worker, recovers an interrupted manifest and its temporary artifacts, and processes the recovered job without opening an external listener."
    - "The verifier retains deterministic cleanup and stops the worker interval after each startup test."
  artifacts:
    - path: src/scripts/verify-episode-artifact-downloads.ts
      provides: Offline live-progress and actual-worker-startup recovery verification.
  key_links:
    - from: startEpisodeArtifactPreparationWorker
      to: initializeEpisodeArtifactPreparations and processNextEpisodeArtifactPreparation
      via: compiled worker startup invocation and returned cleanup callback
      pattern: "startEpisodeArtifactPreparationWorker"
    - from: protected preparation status route
      to: live Archiver progress manifest
      via: repeated direct-router status polling that observes source-byte progress before the published archive is ready
      pattern: "preparing"
---

<objective>
Replace synthetic lifecycle evidence with compiled observations of live Archiver progress and actual background-worker startup recovery.

Purpose: make ZIP-03, ZIP-06, and ZIP-07 independently verifiable while preserving the repository-native offline test model.
Output: deterministic live lifecycle and worker-startup assertions in the existing compiled verifier.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-return-zip-progress-to-user/13-CONTEXT.md
@.planning/phases/13-return-zip-progress-to-user/13-RESEARCH.md
@.planning/phases/13-return-zip-progress-to-user/13-VALIDATION.md
@.planning/phases/13-return-zip-progress-to-user/13-VERIFICATION.md
@.planning/phases/13-return-zip-progress-to-user/13-02-SUMMARY.md
@src/services/episode-artifact-preparation.service.ts
@src/workers/episode-artifact-preparation.worker.ts
@src/scripts/verify-episode-artifact-downloads.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Observe a live Archiver preparing transition through status polling</name>
  <files>src/scripts/verify-episode-artifact-downloads.ts</files>
  <behavior>
    - Create a bounded, sufficiently large canonical final-artifact fixture, queue it through the protected prepare route, and begin real service processing without awaiting completion first.
    - Poll the actual protected status route until it observes a `preparing` state with source-byte-derived progress that is an integer from 1 through 99, or observes a strictly increasing pair of preparing progress values; a zero-only observation is insufficient. While preparing, queue position and download URL are null.
    - Before publication, status remains preparing with progress below 100 and no download URL; after the real processing promise completes and the ZIP has been atomically published, status becomes ready with progress exactly 100 and exposes the download URL.
    - No assertion may manufacture preparation state by rewriting a manifest; cleanup removes the temporary fixture and preparation root in finally handling.
  </behavior>
  <action>Replace the synthetic preparing-manifest branch in the verifier with a live archive scenario. Use a deterministic bounded fixture large enough to emit an Archiver source-byte progress event within the existing command budget, invoke processNextEpisodeArtifactPreparation without awaiting it, and poll the real compiled route stack before awaiting completion. Require either an integer preparing progress in the inclusive 1-99 range or a strictly increasing pair of preparing samples; do not accept a zero-only sample. Assert preparing remains below 100 with no download URL until the processing promise has completed the existing atomic publication path, then assert the published archive's ready status is exactly 100 and exposes the download URL. Retain direct-router invocation rather than opening a listening server. This implements D-02, D-04, D-06, and D-07.</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run verify:episode-artifact-downloads</automated>
  </verify>
  <done>The verifier observes source-byte Archiver progress before publication and proves ready status becomes 100 only after atomic ZIP publication, rather than relying on a fabricated manifest state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise real preparation-worker startup recovery offline</name>
  <files>src/scripts/verify-episode-artifact-downloads.ts</files>
  <behavior>
    - Persist an interrupted preparing job with corresponding partial archive and snapshot fixtures, then invoke the compiled startEpisodeArtifactPreparationWorker export.
    - The returned worker startup path removes incomplete artifacts, requeues and processes the job once, and leaves no second concurrently active preparation.
    - The test calls the returned stop function in finally and performs no network bind or external listener startup.
  </behavior>
  <action>Extend the compiled verifier to import the actual preparation worker and test its exported startup function, not only the service initialization seam. Arrange an interrupted preparing manifest and its job-local temporary artifacts using the established verifier fixtures, call startEpisodeArtifactPreparationWorker, await its startup processing, assert the recovered job reaches the real lifecycle outcome and incomplete files are removed, then stop the returned worker in finally. Keep the existing single-process FIFO assertions and ensure this scenario uses no server bootstrap or external network listener. This implements D-03, D-04, D-08, D-10, and D-12.</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run verify:episode-artifact-downloads &amp;&amp; npm run verify:public-episodes &amp;&amp; npm run verify:summary-runtime-contract &amp;&amp; npm run verify:summary-quality-contract</automated>
  </verify>
  <done>The compiled verifier proves actual worker startup performs interrupted-job recovery and serialized processing offline, with timers and fixtures cleaned up deterministically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker startup → persisted preparation manifests | Restart logic consumes prior filesystem state and must only recover interrupted work. |
| server-side archive processing → status response | Progress is public lifecycle metadata and must represent real server assembly rather than transfer or fabricated state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-12 | Tampering | persisted preparing manifests and partial output | high | mitigate | Exercise actual startup recovery to prove interrupted manifests are requeued and temporary output is removed before reuse. |
| T-13-13 | DoS | worker serialization and recovery | high | mitigate | Verify the real guarded worker startup processes one recovered FIFO job and retains existing no-overlap behavior. |
| T-13-14 | Repudiation | preparation progress status | medium | mitigate | Observe route-exposed progress from a live Archiver run, including pre-ready and ready states, rather than synthetic fixture state. |
| T-13-SC | Tampering | package supply chain | low | accept | No package installation or dependency change is in this closure plan. |
</threat_model>

<source_audit>

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Authenticated preparation, polling progress, and validated cached download | 13-04, 13-05, 13-06 | COVERED | Closure plans address every verifier failure or behavior gap without re-planning verified work. |
| REQ | ZIP-01 | Reuse normalized preparation jobs without duplicate queueing | 13-04 | COVERED | Stale-history active-job regression. |
| REQ | ZIP-02 | Persisted global FIFO and queued position | 13-06 | COVERED | Actual worker startup preserves serialized processing evidence. |
| REQ | ZIP-03 | Byte-derived 0-100 archive progress | 13-06 | COVERED | Live Archiver status observation replaces synthetic state. |
| REQ | ZIP-04 | 24-hour evidence-validated ready reuse | 13-04 | COVERED | Candidate selection preserves revalidation before cache hits. |
| REQ | ZIP-05 | Authenticated validated ready download and legacy migration | prior 13-02/13-03 | VERIFIED | No verifier gap; intentionally not re-planned. |
| REQ | ZIP-06 | Restart recovery, cleanup, final-only/no-path safety | 13-06 | COVERED | Actual worker-startup recovery remains offline. |
| REQ | ZIP-07 | Accurate OpenAPI and compiled lifecycle coverage | 13-04, 13-05, 13-06 | COVERED | Regression, selector parity, and live worker/progress proofs. |
| RESEARCH | — | Reuse active manifest by cache key and validate ready evidence | 13-04 | COVERED | Follows persisted FIFO/cache recommendation. |
| RESEARCH | — | Fixed selector CSV parser and offline compiled verifier | 13-05 | COVERED | Documentation follows existing parser without new packages. |
| RESEARCH | — | Startup rehydration and Archiver progress evidence | 13-06 | COVERED | Tests actual worker and live archive processing. |
| CONTEXT | D-01 | Async prepare/status/download lifecycle | 13-05, 13-06 | COVERED | Contract parity and route-status observation. |
| CONTEXT | D-02 | Progress separate from ZIP transfer | 13-06 | COVERED | Live status polling only. |
| CONTEXT | D-03 | One global preparation | 13-06 | COVERED | Actual guarded worker recovery exercise. |
| CONTEXT | D-04 | Five lifecycle states | 13-06 | COVERED | Live preparing and recovery state coverage. |
| CONTEXT | D-05 | Valid normalized cache hit and idempotency | 13-04, 13-05 | COVERED | Candidate reuse and selector parity. |
| CONTEXT | D-06 | Source-byte assembly percentage | 13-06 | COVERED | Live Archiver transition assertion. |
| CONTEXT | D-07 | Queue position and ready-only download URL | 13-06 | COVERED | Live status field assertions. |
| CONTEXT | D-08 | 24-hour availability | 13-04, 13-06 | COVERED | Expired history handling and startup retention. |
| CONTEXT | D-09 | Source-change cache invalidation | 13-04 | COVERED | Invalidation/retry regression. |
| CONTEXT | D-10 | requireAuth and AUTH_BYPASS behavior | 13-05, 13-06 | COVERED | Route parity continues using protected compiled stack. |
| CONTEXT | D-11 | Fixed final-only allowlist | 13-05 | COVERED | Schema parity stays bounded by the existing selector catalog. |
| CONTEXT | D-12 | Selector-only lifecycle logging | 13-06 | COVERED | Worker recovery runs under existing safe lifecycle logging. |

</source_audit>

<verification>
Run the focused compiled verifier repeatedly until the live transition and worker startup assertions are deterministic, then run the full existing compiled contract suite. The live assertion must observe 1-99 or an increase from source-byte Archiver progress before publication; a fabricated preparing manifest, zero-only progress observation, direct service-only recovery invocation, unclosed worker timer, or network listener invalidates this plan.
</verification>

<success_criteria>
The compiled verifier provides automated, offline evidence for source-byte Archiver progress before published-ready 100 and real worker startup/recovery, closing the behavior-unverified ZIP-03 and ZIP-06 items while preserving the existing final-only and authentication boundaries.
</success_criteria>

## Artifacts

- `src/scripts/verify-episode-artifact-downloads.ts` — live progress and actual worker-startup recovery verification.

<output>
Create `.planning/phases/13-return-zip-progress-to-user/13-06-SUMMARY.md` when done.
</output>
