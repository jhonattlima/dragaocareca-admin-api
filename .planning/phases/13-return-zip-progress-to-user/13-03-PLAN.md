---
phase: 13-return-zip-progress-to-user
plan: 03
type: execute
wave: 2
depends_on: [13-01, 13-02]
files_modified:
  - src/docs/openapi.ts
  - src/scripts/verify-episode-artifact-downloads.ts
autonomous: true
requirements: [ZIP-03, ZIP-04, ZIP-05, ZIP-07]
must_haves:
  truths:
    - "Administrators can discover the authenticated asynchronous lifecycle, polling/no-store behavior, state model, expiry behavior, and direct-route migration from OpenAPI."
    - "The compiled repository-native verifier proves the full lifecycle against compiled route and service code without a localhost listener."
    - "Verification proves no selector/path regression can expose unapproved sources or stale/partial ZIP output, including byte changes that preserve size and timestamps."
  artifacts:
    - path: src/docs/openapi.ts
      provides: "Bearer-protected preparation, status, ready-download, and legacy-migration OpenAPI operations."
    - path: src/scripts/verify-episode-artifact-downloads.ts
      provides: "End-to-end compiled verification for lifecycle, cache, security, and ZIP contract behavior."
  key_links:
    - from: src/docs/openapi.ts
      to: src/routes/episodes.routes.ts
      via: "documented paths, methods, statuses, security, headers, and status schema match mounted handlers."
      pattern: "/v1/episodes/\\{episodeId\\}/artifacts/(prepare|preparations)"
    - from: src/scripts/verify-episode-artifact-downloads.ts
      to: episodesRouter and swaggerSpec
      via: "direct route-stack invocation and OpenAPI inspection run after TypeScript compilation."
      pattern: "episodesRouter|swaggerSpec"
  prohibitions:
    - "OpenAPI and status schemas must not disclose source paths, snapshot locations, cache keys, fingerprints, or internal errors."
    - "The verifier must not require a new runner, network listener, package, or manual-only coverage for lifecycle security behavior."
    - "Documentation must not represent preparation percentage as browser transfer progress."
---

<objective>
Document the complete migration-safe asynchronous contract and make the compiled verifier prove it end to end.

Purpose: give callers an accurate integration contract and prevent regressions in the lifecycle's security, cache, and ZIP guarantees.
Output: OpenAPI lifecycle documentation and final compiled contract coverage.

This plan covers ZIP-03, ZIP-04, ZIP-05, and ZIP-07 through documentation and executable coverage for D-01 through D-12, with particular focus on D-02, D-06, D-07, D-08, D-09, D-10, D-11, and D-12.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-return-zip-progress-to-user/13-CONTEXT.md
@.planning/phases/13-return-zip-progress-to-user/13-RESEARCH.md
@.planning/phases/13-return-zip-progress-to-user/13-VALIDATION.md
@.planning/phases/13-return-zip-progress-to-user/13-PATTERNS.md
@.planning/phases/12-secure-episode-artifact-downloads/12-VERIFICATION.md
@.planning/phases/13-return-zip-progress-to-user/13-01-SUMMARY.md
@.planning/phases/13-return-zip-progress-to-user/13-02-SUMMARY.md

<interfaces>
The public status representation has opaque `jobId`; episode ID; normalized requested, available, and missing selector lists; `queued|preparing|ready|failed|expired` state; integer preparation progress; separate state text; conditional queue position; conditional ready-only download URL; and expiry timestamp. The ready-download response retains Phase 12 canonical ZIP filename, entry names, and selector-only missing header. The direct legacy path is an authenticated migration response, not a ZIP operation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the direct-download OpenAPI operation with the asynchronous lifecycle contract</name>
  <files>src/docs/openapi.ts</files>
  <read_first>
    - src/docs/openapi.ts:702-763: existing artifact-download operation, bearer security, selector parameter, ZIP response, and missing-artifact header format
    - src/routes/episodes.routes.ts: Plan 13-02 lifecycle route paths, status codes, headers, and legacy migration response
    - .planning/phases/13-return-zip-progress-to-user/13-PATTERNS.md: OpenAPI assignment at lines 207-224
    - .planning/phases/13-return-zip-progress-to-user/13-RESEARCH.md: API Contract Recommendation and recommended status body
  </read_first>
  <action>Replace the old live-download OpenAPI operation with bearer-protected `POST /v1/episodes/{episodeId}/artifacts/prepare`, `GET /v1/episodes/{episodeId}/artifacts/preparations/{jobId}`, and `GET /v1/episodes/{episodeId}/artifacts/preparations/{jobId}/download` operations. Reuse the strict selector parameter wording and describe normalized selections, `200` ready-cache and `202` queued/preparing preparation responses, status no-store polling, queue position only while queued, byte-based ZIP assembly percentage separate from state text, ready-only download URL, 24-hour expiry, and `409`/`410` JSON download errors. Retain the canonical ZIP filename and selector-only missing header on the ready-download operation. Document the former direct route as deprecated, authenticated, and `410` with the prepare endpoint migration URL for one release. Define or reuse an API status schema that contains only public fields, omitting paths, manifests, source fingerprints, cache keys, and internal failure details; do not add dependencies.</action>
  <verify>
    <automated>npm run build && npm run verify:episode-artifact-downloads</automated>
  </verify>
  <acceptance_criteria>OpenAPI exposes exactly the lifecycle and legacy migration paths implemented by Plan 13-02, declares bearer security/no-store behavior/status conditions, and exposes no internal filesystem or manifest data.</acceptance_criteria>
  <done>API consumers have a precise documented contract for preparation progress and completed-download behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete compiled direct-router verification of the lifecycle and migration contract</name>
  <files>src/scripts/verify-episode-artifact-downloads.ts</files>
  <read_first>
    - src/scripts/verify-episode-artifact-downloads.ts: Wave 0 lifecycle checks and existing MemoryResponse/route-stack helper conventions
    - src/routes/episodes.routes.ts: Plan 13-02 static route stack, middleware order, and route error contracts
    - src/docs/openapi.ts: Plan 13-03 operation and status-schema definitions
    - .planning/phases/13-return-zip-progress-to-user/13-VALIDATION.md: all remaining verification rows and full-suite command
  </read_first>
  <behavior>
    - Unauthenticated prepare, status, download, and legacy requests return the existing authentication error; development bypass still permits the protected test flow.
    - Equivalent normalized prepare requests reuse one job; a second distinct request queues behind the active job and reports its position; preparing progress is integral and separate from state text.
    - A ready job exposes a download URL and returns a canonical ZIP with selector-only missing header; queued/preparing download is `409`, expired download is `410`, and unknown/mismatched job is `404`.
    - A streaming SHA-256 change or a formerly missing artifact appearing invalidates the ready cache before both status and download; replacing selected bytes with same-size content and restoring timestamps also invalidates it, while restart recovery and 24-hour cleanup retain no usable stale output.
    - OpenAPI matches all three lifecycle operations plus the deprecated direct migration response, and captured JSON/log-facing assertions contain no media-root path.
  </behavior>
  <action>Turn the Wave 0 service assertions into complete compiled direct-router coverage, per D-01 through D-12 and the resolved Phase 13 research decisions. Extend the fake request shape for POST/status/download route params and query values, locate each concrete route stack with its auth middleware, and invoke compiled handlers without starting a listener. Use deterministic service processing/recovery seams so the verifier observes FIFO position, preparing progress, atomic ready state, cache hits, expiry, restart recovery, and cache invalidation from the complete streaming-SHA-256-or-missing selector map. Create a ready archive, then replace one selected source with different bytes of the same length and restore its timestamps before invoking both the status handler and download handler; assert neither can reuse or open the old archive. Separately assert missing-to-present invalidation. Assert all lifecycle JSON carries `Cache-Control: no-store`; assert only ready status carries a download URL; inspect ready ZIP entry names and the retained missing-selector header; assert `409`, `410`, `404`, selector `400`, and auth paths. Assert the legacy direct path remains behind `requireAuth` and responds for one release with `410 Gone` JSON that names `POST /v1/episodes/:episodeId/artifacts/prepare`. Inspect `swaggerSpec` for every documented operation/security/status/header and serialize captured response/log data only in ways that prove the media root is absent. Retain `finally` cleanup, the compiled-script guard, existing verifier command, and no new packages.</action>
  <verify>
    <automated>npm run typecheck && npm run build && npm run verify:public-episodes && npm run verify:episode-artifact-downloads && npm run verify:summary-runtime-contract && npm run verify:summary-quality-contract</automated>
  </verify>
  <acceptance_criteria>The full compiled verification suite passes, and the focused verifier proves auth, the one-release authenticated legacy `410 Gone` JSON migration endpoint, queue/progress, cache validity using streaming SHA-256 plus explicit missing markers, invalidation after same-size/timestamp-preserved byte replacement before both status and download, restart recovery, expiry, OpenAPI parity, final-only canonical ZIP content, and no-path disclosure.</acceptance_criteria>
  <done>The asynchronous artifact-download contract is executable, documented, and protected by repository-native regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenAPI consumer to lifecycle API | Documentation can accidentally advertise unsafe or unavailable behavior. |
| compiled verifier to production contract | Incomplete tests can certify an insecure route or stale-cache regression. |
| captured status/error data to developer output | Test diagnostics can leak the media layout if assertions print internals. |

## STRIDE Threat Register — ASVS L1

High-severity threats block plan completion.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-09 | Spoofing | documented lifecycle routes | high | mitigate | Verify bearer security on every lifecycle and migration operation and invoke actual auth middleware in compiled tests. |
| T-13-10 | Information disclosure | OpenAPI/status/verifier diagnostics | high | mitigate | Document public-only status fields and assert media-root/path values do not appear in captured API/log outputs. |
| T-13-11 | Tampering | cache and ready-download regression coverage | high | mitigate | Verify streaming SHA-256 and missing-marker invalidation before status/download, including same-size/timestamp-preserved byte replacement, atomic ready behavior, and expired/non-ready refusal. |
| T-13-12 | Denial of service | polling/cache contract | medium | mitigate | Verify no-store responses, queue position semantics, and single-worker behavior from the real service/route stack. |
| T-13-13 | Repudiation | legacy migration behavior | low | accept | OpenAPI and executable response assertions make the one-release migration behavior explicit; audit records remain out of scope. |
</threat_model>

<verification>
Run the full compiled suite in the task command, then rerun the focused artifact verifier after any correction. Treat a missing lifecycle path, mismatched OpenAPI method/status, an unauthenticated or incorrectly shaped legacy `410` response, leaked media-root text, successful non-ready download, cache reuse after a digest or missing-marker mismatch including same-size/timestamp-preserved byte replacement, unauthenticated success, or a second active preparation as a blocking failure.
</verification>

<success_criteria>
The documented and compiled contract makes progress polling truthful, preserves Phase 12 final-only ZIP guarantees, blocks high-severity lifecycle threats at ASVS L1, and gives direct-route consumers an explicit migration path.
</success_criteria>

## Artifacts this phase produces

- Updated `src/docs/openapi.ts` — lifecycle, security, no-store, expiration, and migration API documentation.
- Final `src/scripts/verify-episode-artifact-downloads.ts` — compiled end-to-end regression coverage.

<output>
Create `.planning/phases/13-return-zip-progress-to-user/13-03-SUMMARY.md` when done.
</output>
