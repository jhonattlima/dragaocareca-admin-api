---
phase: 17-trailer-metadata-publication-and-retention
plan: 05
type: execute
wave: 5
depends_on: [17-04]
files_modified:
  - src/scripts/verify-youtube-trailer-publication.ts
  - src/services/youtube-trailer-publication.service.ts
  - src/services/episode-trailer-retention.service.ts
autonomous: true
gap_closure: true
requirements: [TRAILER-04, TRAILER-07, TRAILER-08]
requirements_addressed: [TRAILER-04, TRAILER-07, TRAILER-08]
must_haves:
  truths:
    - "The offline verifier executes playlist-insertion failure and public-update failure through the fake provider, proves the provider video remains private, and proves the same publication job can be retried successfully."
    - "The offline verifier injects a filesystem deletion failure after confirmed public publication, proves every eligible local version remains present, preserves the canonical URL/publication state, records retryable cleanup, and proves a later retry completes cleanup."
    - "The publication boundary rejects Unicode titles over 100 code points and the complete title/hashtag policy before any provider event, while accepting the exact valid boundary."
    - "The existing happy path, repeat/idempotency, auth/no-store, OAuth tripwire, artifact-download, draft lifecycle, private-job, typecheck, build, and regression checks remain executable and passing."
  artifacts:
    - path: "src/scripts/verify-youtube-trailer-publication.ts"
      provides: "Repository-native fake-provider, filesystem-failure, metadata-boundary, and regression assertions"
      contains: "playlist failure, public-update failure, deletion failure, Unicode boundary"
    - path: "src/services/youtube-trailer-publication.service.ts"
      provides: "Server-side metadata policy enforcement before provider delegation"
    - path: "src/services/episode-trailer-retention.service.ts"
      provides: "Rollback-safe retention behavior exercised under injected unlink failure"
  key_links:
    - from: "src/scripts/verify-youtube-trailer-publication.ts"
      to: "src/services/youtube-trailer-publication.service.ts"
      via: "fresh ready-job fixtures invoke publishYoutubeTrailer with a configurable fake provider"
    - from: "src/scripts/verify-youtube-trailer-publication.ts"
      to: "src/services/episode-trailer-retention.service.ts"
      via: "temporary media fixture and injected fs.promises.unlink failure exercise retainEpisodeTrailerVersions"
    - from: "package.json"
      to: "dist/scripts/verify-youtube-trailer-publication.js"
      via: "npm run verify:youtube-trailer-publication"
---

<objective>
Close the Phase 17 verification gaps by making provider and filesystem failure recovery, Unicode metadata limits, and the complete title/hashtag policy executable in the existing offline verifier.

Purpose: The implementation already contains the private-first and rollback-safe boundaries, but the phase cannot be accepted until those invariants are demonstrated through the repository-native fake provider and disposable filesystem fixture.
Output: One executable gap-closure verifier extension plus any narrowly required service-side policy guard, followed by the complete offline regression evidence.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-trailer-metadata-publication-and-retention/17-CONTEXT.md
@.planning/phases/17-trailer-metadata-publication-and-retention/17-VERIFICATION.md
@.planning/phases/17-trailer-metadata-publication-and-retention/17-02-SUMMARY.md
@.planning/phases/17-trailer-metadata-publication-and-retention/17-03-SUMMARY.md
@.planning/phases/17-trailer-metadata-publication-and-retention/17-04-SUMMARY.md
@src/scripts/verify-youtube-trailer-publication.ts
@src/services/youtube-trailer-publication.service.ts
@src/services/episode-trailer-retention.service.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add executable failure-injection and metadata-boundary coverage</name>
  <files>src/scripts/verify-youtube-trailer-publication.ts, src/services/youtube-trailer-publication.service.ts, src/services/episode-trailer-retention.service.ts</files>
  <read_first>
    - src/scripts/verify-youtube-trailer-publication.ts: FakeYoutubeTrailerPublicationProvider, prepareReadyPublicationJob, verifyPublicationAndRetention, and main fixture cleanup
    - src/services/youtube-trailer-publication.service.ts: assembledTitle, publishYoutubeTrailer failure handling, retry path for public_confirmed jobs, and providerMethod
    - src/services/episode-trailer-retention.service.ts: discoverTrailerVideoVersions, removeWithRollback, and retainEpisodeTrailerVersions
    - .planning/phases/17-trailer-metadata-publication-and-retention/17-VERIFICATION.md: gaps.missing and failed truth
    - .planning/phases/17-trailer-metadata-publication-and-retention/17-CONTEXT.md: D-02, D-06, D-11, and D-12
  </read_first>
  <action>
    Extend the existing disposable verifier; do not create a second test harness or call a live provider. Per D-02, add a fake-provider switch for public-update failure that throws before changing privacy, and use the existing playlist-insert switch to run a fresh ready-job fixture through playlist failure. For each failure, assert the fake provider video is still private, no canonical URL is persisted, publication state is recoverable, and clearing the injected failure allows retrying the same job/provider video to reach public confirmation without a duplicate video or playlist item. Keep the existing success and repeat event-order assertions.

    Per D-06, add direct service-boundary assertions for an exact 100-code-point valid title, a title over 100 Unicode code points including astral characters, empty/whitespace title, angle-bracket title, malformed hashtag, invalid hashtag characters, and more than the allowed three hashtags. Assert every invalid case rejects before the fake provider records any event; if the service does not yet enforce the three-hashtag maximum, add that guard in assembledTitle while retaining the route's existing strict schema. Do not add automatic hashtag generation or Phase 18 lookup behavior.

    Per D-11 and D-12, create a separate publication fixture with more eligible prior files than the keep count, patch only the disposable fixture's fs.promises.unlink operation to fail on the first eligible deletion, and invoke the real publication service. Assert the provider is public, the episode URL remains canonical, the job publication status remains public_confirmed, retention is retryable-error, and all eligible files are still present. Restore unlink, retry the same publication job, and assert retention becomes complete with current plus twelve prior versions preserved. Ensure the injected function is restored in finally and the existing temporary-root cleanup remains intact. If the retention implementation needs a test seam to make this deterministic, keep it limited to dependency injection of filesystem operations and preserve the default production fs implementation and rollback behavior.
  </action>
  <acceptance_criteria>
    - The verifier has executable playlist-insert and public-update failure scenarios, and both scenarios prove private provider state plus successful retry of the same job.
    - The verifier has an executable deletion-failure scenario proving no eligible version loss, intact public URL/publication state, retryable cleanup state, and successful cleanup retry.
    - Unicode-over-100 and exact-boundary title checks, empty/invalid title checks, and complete hashtag policy checks all fail before provider delegation; valid boundary input remains accepted.
    - No live YouTube, OAuth, HTTP, or network write is reachable from the scenarios; the existing global tripwire still guards the run.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run verify:youtube-trailer-publication</automated>
  </verify>
  <done>The shared compiled verifier executes every reported failure and metadata gap against temporary fixtures, and the service/retention behavior satisfies the locked private-first and success-gated recovery invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Run the complete gap-closure regression gate</name>
  <files>src/scripts/verify-youtube-trailer-publication.ts, .planning/phases/17-trailer-metadata-publication-and-retention/17-VALIDATION.md</files>
  <read_first>
    - package.json: verify:youtube-trailer-publication and existing regression script definitions
    - .planning/phases/17-trailer-metadata-publication-and-retention/17-VALIDATION.md: existing command/evidence format and manual readiness boundary
    - .planning/phases/17-trailer-metadata-publication-and-retention/17-VERIFICATION.md: required retained happy-path, idempotency, auth/no-store, OAuth tripwire, and regression evidence
    - src/scripts/verify-youtube-trailer-publication.ts: final main() command coverage and network/OAuth cleanup
  </read_first>
  <action>
    Run and record the full offline gate after Task 1. Preserve the existing happy path, repeat/idempotency, auth/no-store, OAuth/network tripwire, artifact-download, draft reservation/staging/promotion/rollback, private YouTube job, and route/OpenAPI regression checks. Update only the execution-evidence portion of 17-VALIDATION.md if the repository workflow requires recording the new passing scenarios; keep the manual OAuth scope/channel/playlist readiness boundary explicit, keep YOUTUBE_TRAILER_PUBLICATION_ENABLED disabled outside the temporary verifier fixture, and do not perform live YouTube writes or enable publication. Per TRAILER-04, TRAILER-07, and TRAILER-08, report the exact commands and the failure-injection assertions in the validation evidence.
  </action>
  <acceptance_criteria>
    - The complete suite passes: typecheck, build, episode artifact downloads, trailer upload lifecycle, private YouTube job lifecycle, and the Phase 17 publication verifier.
    - The recorded evidence names playlist insertion failure, public-update failure, retention deletion failure/retry, Unicode-over-100, and title/hashtag policy checks.
    - The final evidence confirms no live YouTube write or publication enablement occurred.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run verify:episode-artifact-downloads &amp;&amp; npm run verify:trailer-video-upload-lifecycle &amp;&amp; npm run verify:youtube-trailer-job-lifecycle &amp;&amp; npm run verify:youtube-trailer-publication</automated>
  </verify>
  <done>TRAILER-04, TRAILER-07, and TRAILER-08 have executable offline gap-closure evidence while all required regressions and the no-live-write boundary remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verifier -> fake provider | Failure switches must model provider errors without permitting real YouTube calls. |
| Verifier -> temporary filesystem | Deletion failure injection must affect only disposable fixture files and restore the original fs operation. |
| Publication input -> service | Metadata must be rejected before any provider side effect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-05-01 | Tampering | fake provider failure switches | high | mitigate | Assert playlist and public-update failures occur before public state, then retry the same provider ID and verify no duplicate side effect. |
| T-17-05-02 | Data Loss | retention deletion injection | high | mitigate | Use only unique temporary media roots, inject one unlink failure, assert rollback restores every eligible file, and restore the original unlink function in finally. |
| T-17-05-03 | Tampering | metadata validation boundary | high | mitigate | Exercise Unicode code-point limit, empty/forbidden title, hashtag syntax, and hashtag-count rejection before provider invocation. |
| T-17-05-SC | Tampering | npm/pip/cargo installs | low | accept | No package installation is introduced by this gap-closure plan. |
</threat_model>

<verification>
The executable gate is the complete command sequence in Task 2. It must pass with temporary SQLite/media roots, a fake provider, and the network/OAuth tripwire; no live YouTube write or publication enablement is part of this plan.
</verification>

<success_criteria>
The three reported verification gaps are closed with repository-native executable evidence, while the existing lifecycle, security, metadata, retention, typecheck, build, and regression checks remain passing.
</success_criteria>

<source_audit>
| Source | ID | Coverage in this gap-closure plan |
|--------|----|-----------------------------------|
| GOAL | — | Task 1 and Task 2 close the missing executable failure-boundary evidence for the phase goal. |
| REQ | TRAILER-04 | Task 1 proves private-first failure/retry, same-provider idempotency, and canonical URL preservation. |
| REQ | TRAILER-07 | Task 1 proves deletion rollback, public-state preservation, retryable cleanup, and current-plus-twelve retention. |
| REQ | TRAILER-08 | Task 1 and Task 2 preserve verifier, auth/no-store, OAuth tripwire, typecheck, build, and regression evidence. |
| RESEARCH | R-04/R-05/R-06/R-07 | Task 1 exercises private-first compensation, Unicode/hashtag validation, server-derived retention, and the existing offline fixture/tripwire. |
| CONTEXT | D-02 | Task 1 proves failed playlist/public-update paths remain private and retryable. |
| CONTEXT | D-06 | Task 1 proves the complete Unicode title and hashtag boundary before provider delegation. |
| CONTEXT | D-11 | Task 1 proves retention is reached only after confirmed public publication and playlist insertion. |
| CONTEXT | D-12 | Task 1 proves no eligible files are lost on cleanup failure and that retry succeeds. |
</source_audit>

<output>
Create `.planning/phases/17-trailer-metadata-publication-and-retention/17-05-SUMMARY.md` when done.

Artifacts this phase produces:

- Updated `src/scripts/verify-youtube-trailer-publication.ts` with provider, filesystem, and metadata failure-boundary assertions.
- Any narrowly required server-side title/hashtag policy guard or retention test seam in the named service files.
- Updated Phase 17 validation evidence showing the complete offline regression gate.
</output>
