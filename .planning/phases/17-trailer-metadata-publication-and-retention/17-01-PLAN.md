---
phase: 17-trailer-metadata-publication-and-retention
plan: 01
type: execute
wave: 1
depends_on: [17-00]
files_modified:
  - src/database/sqlite.ts
  - src/database/repositories/youtube-trailer-job.repository.ts
  - src/schemas/episode.ts
  - src/database/repositories/episode.repository.ts
  - src/services/youtube-trailer-upload.provider.ts
  - src/scripts/verify-youtube-trailer-publication.ts
autonomous: true
requirements: [TRAILER-04, TRAILER-08]
must_haves:
  truths:
    - A ready job has durable publication milestones, metadata identity, canonical URL, and independently recoverable retention error state.
    - Provider publication operations are internal-only and can read/reconcile the same provider video and playlist item without exposing credentials, IDs, sessions, or raw responses.
    - Live readiness requires both the existing `youtube.upload` scope and the edit-capable `youtube.force-ssl` scope, with a fail-closed token assertion.
    - Existing published URLs and local replacement/manual-sync state survive schema initialization and source obsolescence.
  artifacts:
    - src/database/sqlite.ts
    - src/database/repositories/youtube-trailer-job.repository.ts
    - src/services/youtube-trailer-upload.provider.ts
  key_links:
    - Publication persistence uses the existing source fingerprint, revision, and lease predicates.
    - Provider methods map YouTube Data API responses into normalized internal types before the service layer sees them.
---

<objective>
Establish the durable publication/retention state contract and the server-only YouTube provider operations that later plans will orchestrate.

Purpose: Publication must survive retries, restarts, timeouts, and local trailer replacement without duplicate provider videos or stale URL writes. This plan creates the contracts without changing the existing private upload workflow.
Output: Backward-compatible SQLite/repository state, episode publication mapping, and an injectable provider interface plus live implementation for metadata, playlist reconciliation, privacy transition, and readiness.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-trailer-metadata-publication-and-retention/17-CONTEXT.md
@.planning/phases/17-trailer-metadata-publication-and-retention/17-RESEARCH.md
@.planning/phases/17-trailer-metadata-publication-and-retention/17-PATTERNS.md
@src/database/sqlite.ts
@src/database/repositories/youtube-trailer-job.repository.ts
@src/services/youtube-trailer-job.service.ts
@src/services/youtube-trailer-upload.provider.ts
@src/scripts/verify-youtube-trailer-publication.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add guarded publication state and episode contract fields</name>
  <files>src/database/sqlite.ts, src/database/repositories/youtube-trailer-job.repository.ts, src/schemas/episode.ts, src/database/repositories/episode.repository.ts, src/scripts/verify-youtube-trailer-publication.ts</files>
  <action>Per D-04, D-07, D-08, D-11, and D-12, extend the existing youtube_trailer_jobs schema and compatibility column initializer without deleting or resetting existing rows. Persist publication state/milestones for metadata accepted, playlist membership confirmed, public confirmation, canonical URL, cleanup status/error/timestamps, and the metadata snapshot or digest required to reconcile repeated requests. Add repository row types, explicit mapping, create/read/update operations, and a publication lease/CAS transition keyed by the exact job ID, episode, source filename/SHA-256/byte count, revision, and lease ID. Keep provider internals out of episode DTO state; preserve the existing episode.youtube URL and trailerVideoSyncStatus mapping, including manual-sync-required after a replacement. Use constrained status values and nullable defaults so old databases initialize safely. Extend the Wave 0 temporary SQLite fixture assertions rather than creating a second fixture path.</action>
  <verify><automated>npm run typecheck && npm run verify:youtube-trailer-publication</automated></verify>
  <done>Existing private-job lifecycle still typechecks; the Wave 0 temporary database initializes against old/new schemas; publication milestones and cleanup errors are durable, source-guarded, and mapped without leaking session/path/provider internals.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the internal YouTube Data API provider boundary</name>
  <files>src/services/youtube-trailer-upload.provider.ts</files>
  <action>Per D-01, D-02, D-03, D-05, and D-07, extend the injected YoutubeTrailerUploadProvider contract and LiveYoutubeTrailerUploadProvider with internal operations for reading a video, updating complete snippet metadata when needed, listing playlist membership, inserting an absent playlist item, and narrowly updating privacy status. Require the configured Dragao Careca channel and playlist values at the server boundary, and make readiness inspect `getTokenInfo().scopes` (or the equivalent normalized grant result) and fail closed unless both exact scopes `youtube.upload` and `youtube.force-ssl` are present. Missing token, missing scope, token-info failure, channel mismatch, playlist mismatch, or disabled publication must prevent live writes and return a sanitized retryable readiness category. Preserve the existing private upload methods. Build complete valid snippet payloads with the exact final saved summary as description, operator title/hashtags, and `snippet.categoryId` preserved from the same video's authoritative `videos.list` response; only use an explicitly configured bounded fallback category when that read has no category. Do not accept browser-supplied provider IDs or playlist IDs, and do not add Phase 18 authoring. Return only normalized internal records. Implement read-before-write reconciliation and treat timeout/ambiguous responses as recoverable reads.</action>
  <verify><automated>npm run build && npm run verify:youtube-trailer-publication</automated></verify>
  <done>The provider exposes deterministic injected operations for private-read, complete metadata update, playlist-read/insert, public update, and public-read; readiness proves both `youtube.upload` and `youtube.force-ssl` and rejects every missing-scope/configuration case before a live write; categoryId is preserved; no provider/session/token/raw response crosses the service DTO boundary.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service -> SQLite | Internal publication state crosses into durable storage and must be source/revision guarded. |
| Service -> YouTube Data API | OAuth-authenticated provider side effects and external identifiers cross the server boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | publication repository CAS | high | mitigate | Require exact job/source/revision/lease predicates for every publication milestone and preserve manual-sync source obsolescence. |
| T-17-02 | Information Disclosure | provider boundary and persisted errors | high | mitigate | Keep tokens, session URIs, provider IDs, raw responses, paths, and raw provider messages internal; persist/expose only normalized categories and timestamps. |
| T-17-03 | Elevation of Privilege | OAuth readiness/provider config | high | mitigate | Verify edit-capable scope and configured channel/playlist server-side; never trust request-supplied provider or playlist authority. |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install is introduced by this plan; existing dependencies are reused. |
</threat_model>

<verification>
Run `npm run typecheck` and `npm run build`; inspect the schema initializer for additive compatibility behavior and confirm no live provider call is reachable from a verifier until the injected fake provider is supplied.
</verification>

<success_criteria>
Publication has a durable, guarded internal contract and the provider can perform every required Data API read/write operation while retaining the existing private-first job behavior.
</success_criteria>

<output>
Create `.planning/phases/17-trailer-metadata-publication-and-retention/17-01-SUMMARY.md` when done.
</output>
