---
phase: 10-summary-generation-workflow-and-admin-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/episode-transcription.service.ts
  - src/services/episode-summary.service.ts
autonomous: true
requirements:
  - SUMM-03
  - FLOW-01
  - FLOW-03
must_haves:
  truths:
    - Transcript completion and transcript regeneration automatically queue a new summary draft job, and stale in-flight summary runs are superseded by the transcript version that produced them per D-01, D-02, D-06, and D-12.
    - The summary workflow keeps the shared `episode.state.json` contract as the source of truth for draft state, while `summary.txt` remains the separate draft artifact beside the episode files and SQLite `episodes.summary` stays untouched per D-07, D-08, and D-09.
    - Summary status and progress stay aligned with the transcript workflow vocabulary so later admin-web code can reuse the same mental model without a second backend contract per D-04, D-10, and D-11.
  artifacts:
    - src/services/episode-transcription.service.ts
    - src/services/episode-summary.service.ts
  key_links:
    - transcript completion -> summary queueing
    - transcript invalidation or replacement -> summary abort plus rerun
    - `episode.state.json` version gate -> stale summary run suppression
---

<objective>
Wire the transcript workflow so successful completion and transcript reruns automatically drive summary generation, while preserving the draft/file boundary and keeping the backend sequential and VPS-friendly.

Purpose: Make summary generation happen from the transcript lifecycle itself, not from a manual refresh path, so the backend owns the whole draft-summary orchestration per D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, and D-15.
Output: Transcript completion and transcript replacement both manage summary draft queueing/abort behavior, and the summary service exposes the draft-state access the admin API will read in the next plan.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-summary-generation-workflow-and-admin-api/10-CONTEXT.md
@docs/SDD.md
@src/services/episode-transcription.service.ts
@src/services/episode-summary.service.ts
@src/services/episode-media-layout.service.ts
@src/schemas/episode-draft-state.ts
</context>

<source_audit>
## Multi-Source Coverage Audit

- GOAL: Covered by automatic transcript-driven summary orchestration and draft storage that never writes the suggestion into SQLite final summary data.
- REQ `SUMM-03`: Covered by summary draft generation remaining separate from the final episode summary field.
- REQ `FLOW-01`: Covered by the sequential backend runtime path that continues to use the existing single-worker summary service.
- REQ `FLOW-03`: Covered by keeping the generated summary as a draft artifact in the episode folder and shared episode state file.
- RESEARCH: Covered by the existing `episode-transcription.service.ts`, `episode-summary.service.ts`, and `episode-media-layout.service.ts` code surface already implementing the draft workflow primitives.
- CONTEXT: Covered by D-01, D-02, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, and D-15; D-03 is honored by avoiding any manual refresh path.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Auto-drive summary generation from transcript completion and reruns</name>
  <read_first>@docs/SDD.md, @.planning/phases/10-summary-generation-workflow-and-admin-api/10-CONTEXT.md, @src/services/episode-transcription.service.ts, @src/services/episode-summary.service.ts, @src/services/episode-media-layout.service.ts, @src/schemas/episode-draft-state.ts</read_first>
  <files>src/services/episode-transcription.service.ts, src/services/episode-summary.service.ts</files>
  <acceptance_criteria>
    - When transcript drafting reaches `done`, the backend queues summary generation automatically without any UI action, and the same behavior occurs again after a transcript replacement finishes per D-01, D-02, and D-03.
    - If a transcript is cleared, replaced, or otherwise invalidated before the next transcript run completes, any stale summary draft run is aborted or superseded so an old summary cannot overwrite the new transcript-derived suggestion per D-06 and D-12.
    - The summary draft continues to live in the episode-folder draft contract, with `episode.state.json` tracking summary status/progress and `summary.txt` remaining separate from the canonical SQLite `episodes.summary` field per D-07, D-08, and D-09.
    - Summary status helpers still present the transcript-like `idle`, `pending`, `processing`, `done`, and `error` vocabulary plus progress/error details for later API reuse per D-10 and D-11.
  </acceptance_criteria>
  <action>Update `src/services/episode-transcription.service.ts` so the transcript lifecycle itself becomes the trigger point for summary work: when a draft transcript reaches terminal success, queue summary generation immediately; when the transcript is cleared or replaced, abort or supersede any stale summary draft state before the next transcript run writes a new one; and when the transcript sync path finishes again, keep the summary version keyed to the transcript version so stale output cannot win. Extend `src/services/episode-summary.service.ts` only as needed to expose a clean draft read helper that returns the current summary suggestion plus its status snapshot from the shared episode-state/summary-file contract, without introducing a second persistence model or any direct writes to SQLite `episodes.summary`. Keep the implementation sequential and backend-owned, and do not add a manual refresh branch or frontend dependency while honoring D-01 through D-15.</action>
  <verify>
    <automated>npm run build && npm run verify:summary-runtime-contract</automated>
  </verify>
  <done>Transcript completion, transcript reruns, and transcript invalidation now automatically manage summary draft generation and stale-state cleanup without mutating the final summary column.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript workflow -> summary workflow | Transcript completion and reruns must only queue summary work for the matching transcript version |
| service state -> local filesystem | `episode.state.json` and `summary.txt` must stay separate from the canonical SQLite summary field |
| draft versioning -> stale-run suppression | Version checks prevent old summary runs from overwriting newer transcript-derived output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | `src/services/episode-transcription.service.ts` | high | mitigate | Gate summary queueing on transcript terminal success and clear/replace events so stale runs cannot reuse an outdated transcript version. |
| T-10-02 | Tampering | `src/services/episode-summary.service.ts` | high | mitigate | Keep draft summary writes version-checked and separate from SQLite summary persistence so old output cannot overwrite newer draft state. |
| T-10-03 | Information Disclosure | episode draft files | medium | mitigate | Limit the draft contract to workflow metadata and summary text in the episode folder; do not mirror private state into unrelated stores. |
| T-10-SC | Tampering | npm installs | low | accept | No new package installation is planned for this plan; reuse the existing dependency set only. |
</threat_model>

<verification>
- `npm run build && npm run verify:summary-runtime-contract`
</verification>

<success_criteria>
- A successful transcript run automatically starts summary generation.
- A repeated transcript run retriggers summary generation from the new transcript version.
- Draft summary state remains file-backed and separate from the final SQLite summary column.
</success_criteria>

<output>
Create `.planning/phases/10-summary-generation-workflow-and-admin-api/10-01-SUMMARY.md` when done.
</output>
