---
phase: 10-summary-generation-workflow-and-admin-api
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/routes/episodes.routes.ts
autonomous: true
requirements:
  - API-02
  - API-03
must_haves:
  truths:
    - Authenticated admin clients can read the current summary suggestion and its generation state from the backend without touching the final SQLite summary field per API-02 and API-03.
    - Route responses expose the transcript-like summary state vocabulary, progress, and error details so later admin-web code can bind directly to the backend contract without re-deriving workflow logic per D-10, D-11, and D-12.
    - The backend remains read-only at the HTTP layer for summary drafts in v1.2; there is no manual trigger or refresh endpoint.
  artifacts:
    - src/routes/episodes.routes.ts
  key_links:
    - protected route layer -> summary service read helper
    - route response body -> draft summary file plus status snapshot
---

<objective>
Expose the protected admin API contract for summary retrieval, while keeping the workflow backend-owned and leaving `admin-web` integration for a later milestone.

Purpose: Give operators and future admin-web code a stable read surface for summary drafts without adding a manual refresh UI path or mutating SQLite summary data per API-02, API-03, and D-15.
Output: A protected summary read endpoint and response payloads that surface the draft suggestion, status, progress, and failure details in the existing workflow vocabulary.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-summary-generation-workflow-and-admin-api/10-CONTEXT.md
@.planning/PROJECT.md
@src/routes/episodes.routes.ts
@src/services/episode-summary.service.ts
@src/database/repositories/episode.repository.ts
</context>

<source_audit>
## Multi-Source Coverage Audit

- GOAL: Covered by exposing a protected backend endpoint for summary read, with no frontend implementation in this repo.
- REQ `API-02`: Covered by a protected read endpoint that returns the current summary suggestion together with generation state.
- REQ `API-03`: Covered by route payloads that surface readiness and failure details clearly for later admin-web integration.
- RESEARCH: Covered by the existing `episodes.routes.ts` protected route patterns and the summary service contract already in `src/services/episode-summary.service.ts`.
- CONTEXT: Covered by D-03, D-10, D-11, D-12, and D-15; this plan deliberately does not add any manual-refresh UI path, backend trigger endpoint, or admin-web wiring.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 2: Expose protected summary read endpoint</name>
  <read_first>@.planning/PROJECT.md, @.planning/phases/10-summary-generation-workflow-and-admin-api/10-CONTEXT.md, @src/routes/episodes.routes.ts, @src/services/episode-summary.service.ts, @src/database/repositories/episode.repository.ts</read_first>
  <files>src/routes/episodes.routes.ts</files>
  <acceptance_criteria>
    - `GET /v1/episodes/:episodeId/episodes-generated-summary` exposes a protected read endpoint that returns the current summary suggestion plus its generation state, progress, and error details per API-02 and API-03.
    - The read payload uses the same `idle`, `pending`, `processing`, `done`, and `error` vocabulary as the transcript workflow so future admin-web code can reuse the backend mental model per D-10 and D-11.
    - Endpoint behavior stays backend-owned and does not mutate SQLite `episodes.summary`; it only surfaces the draft suggestion and summary workflow state per D-12 and D-15.
  </acceptance_criteria>
  <action>Add the protected summary route to `src/routes/episodes.routes.ts`, using the summary service read helper rather than duplicating filesystem logic in the router. Return `GET /v1/episodes/:episodeId/episodes-generated-summary` to read the current draft summary suggestion and its status snapshot. Keep the route layer thin, preserve the existing `requireAuth` protection, and avoid introducing any frontend coupling or manual-refresh UI dependency while honoring API-02, API-03, and D-03 through D-15.</action>
  <verify>
    <automated>npm run build && npm run verify:summary-runtime-contract</automated>
  </verify>
  <done>The protected admin API can read summary drafts without exposing a frontend workflow or mutating the final SQLite summary field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated admin client -> protected routes | Untrusted request data selects episode IDs for read access |
| route layer -> summary service | Route handlers must not reimplement draft-file reads or write paths |
| route response -> future admin-web | The response contract must stay stable enough for later frontend binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Spoofing | `src/routes/episodes.routes.ts` | high | mitigate | Keep the summary route behind the existing auth middleware so only authenticated admin callers can reach it. |
| T-10-05 | Tampering | `src/routes/episodes.routes.ts` | high | mitigate | Route handlers should call summary service helpers instead of writing files directly, so the backend keeps one draft-state contract. |
| T-10-06 | Information Disclosure | summary read endpoint | medium | mitigate | Return only the draft suggestion, status, progress, and error details that admin operators need; do not expose unrelated SQLite or media internals. |
| T-10-SC | Tampering | npm installs | low | accept | No new package installation is planned for this plan; reuse the existing dependency set only. |
</threat_model>

<verification>
- `npm run build && npm run verify:summary-runtime-contract`
</verification>

<success_criteria>
  - A protected read endpoint at `/v1/episodes/:episodeId/episodes-generated-summary` returns the current summary suggestion and generation state.
  - The admin API contract remains backend-owned and does not introduce any frontend UI dependency.
</success_criteria>

<output>
Create `.planning/phases/10-summary-generation-workflow-and-admin-api/10-02-SUMMARY.md` when done.
</output>
