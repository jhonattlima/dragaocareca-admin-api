---
phase: 14-reconcile-artifact-job-contract-and-harden-zip-lifecycle
plan: 01
type: execute
autonomous: true
requirements: [ART-01, ART-02, ART-03, ART-04, ART-05, ART-06, ART-07, ZIP-01, ZIP-02, ZIP-03, ZIP-04, ZIP-06, ZIP-07]
---

# Phase 14 Plan 01: Reconcile the authoritative artifact-job API

Preserve the current protected `/v1/episodes/:episodeId/artifacts/jobs` POST, status, and download lifecycle as the authoritative contract. Allow omitted request bodies as default-all, retain nonempty JSON selector arrays, and do not restore CSV or legacy migration routes.

## Tasks

1. Add a compatible SQLite evidence migration and enforce 24-hour cache retention, streamed SHA-256/missing-marker revalidation, byte-derived Archiver progress, atomic 100 publication, and generic failed-job errors.
2. Align routes, OpenAPI, and the compiled direct-router verifier with default-all behavior, evidence invalidation, safe error responses, and the current route set.

## Verification

`npm run typecheck && npm run build && npm run verify:episode-artifact-downloads`
