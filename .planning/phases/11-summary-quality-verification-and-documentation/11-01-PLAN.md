---
phase: 11-summary-quality-verification-and-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/verify-summary-quality-contract.ts
  - package.json
autonomous: true
requirements:
  - SUMM-02
  - OPS-02
must_haves:
  truths:
    - The repo contains an executable backend check that verifies summary outputs stay short, Portuguese-BR, transcript-only, structurally valid, and rejects non-`pt-BR` output.
    - Invalid summary outputs fail closed so the backend contract does not silently accept overlong, underlong, or malformed drafts.
    - Verification runs from compiled backend code and does not depend on live HTTP or frontend interaction.
  artifacts:
    - src/scripts/verify-summary-quality-contract.ts
    - package.json
  key_links:
    - summary service validator -> executable quality contract
    - compiled script -> deterministic quality cases
---

<objective>
Add an executable quality verifier for the summary-suggestion workflow so phase 11 can prove the generated output still matches the published Portuguese-BR draft contract.

Purpose: Convert the summary quality rules into a repo-native check that catches regressions in output length, sentence count, prompt discipline, and transcript-only behavior.
Output: A backend verification script plus npm wiring that can validate good and bad summary outputs from compiled code.
</objective>

<execution_context>
@/home/jhonatt/.codex/gsd-core/workflows/execute-plan.md
@/home/jhonatt/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-summary-quality-verification-and-documentation/11-CONTEXT.md
@.planning/phases/11-summary-quality-verification-and-documentation/11-RESEARCH.md
@.planning/PROJECT.md
@src/services/episode-summary.service.ts
@src/scripts/verify-summary-runtime-contract.ts
</context>

<source_audit>
## Multi-Source Coverage Audit

- GOAL: Covered by executable summary quality verification that exercises the backend service contract directly.
- REQ `SUMM-02`: Covered by assertions on Portuguese-BR output and publish-ready summary length/sentence bounds.
- REQ `OPS-02`: Covered by compiled-output verification with deterministic good/bad cases.
- RESEARCH: Covered by the current summary service and existing runtime verifier surface.
- CONTEXT: Covered by D-01 through D-03, with transcript-only input and no frontend dependency.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add executable summary quality verification</name>
  <read_first>@.planning/PROJECT.md, @.planning/phases/11-summary-quality-verification-and-documentation/11-CONTEXT.md, @.planning/phases/11-summary-quality-verification-and-documentation/11-RESEARCH.md, @src/services/episode-summary.service.ts, @src/scripts/verify-summary-runtime-contract.ts, @package.json</read_first>
  <files>src/scripts/verify-summary-quality-contract.ts, package.json</files>
  <acceptance_criteria>
    - The repo exposes an executable quality verifier for summary outputs that checks short Portuguese-BR summaries, transcript-only prompt discipline, structural validity, and rejection of non-`pt-BR` output.
    - The verifier includes at least one passing case and one or more failing cases so the contract proves both acceptance and rejection behavior.
    - The script runs from compiled output and reuses the existing service contract rather than duplicating summary generation logic.
  </acceptance_criteria>
  <action>Create `src/scripts/verify-summary-quality-contract.ts` and add an npm script such as `verify:summary-quality-contract`. Use the existing summary service helpers to drive deterministic test cases with a fake runtime or focused fixtures. Validate that good summaries are accepted and that malformed outputs are rejected: too short, too long, wrong sentence count, non-`pt-BR`, or otherwise outside the published quality contract. Keep the verifier backend-only and compile-friendly, and do not add frontend coupling or live HTTP dependencies.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build</automated>
    <automated>npm run verify:summary-quality-contract</automated>
  </verify>
  <done>The repo has an executable summary-quality contract that proves the backend accepts only publish-friendly transcript-derived drafts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fake runtime / fixtures -> summary service | The verifier must not trust malformed summary outputs |
| compiled script -> backend contract | Verification should exercise the real service helpers, not duplicated logic |
| quality contract -> operator expectations | The verifier should enforce the published short Portuguese-BR summary shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | `src/scripts/verify-summary-quality-contract.ts` | medium | mitigate | Use deterministic fixtures and service helpers so malformed outputs are rejected by the same contract the backend uses in production. |
| T-11-02 | Information Disclosure | verification fixtures | low | mitigate | Keep fixtures local and avoid logging full transcript bodies or unrelated episode data. |
| T-11-SC | Tampering | npm installs | low | accept | No package installation is planned for this phase; reuse the existing dependency set only. |
</threat_model>

<verification>
- `npm run typecheck`
- `npm run build`
- `npm run verify:summary-quality-contract`
</verification>

<success_criteria>
  - Summary quality is enforced by an executable backend contract.
  - Invalid summary drafts are rejected by the verifier.
  - The phase ends with a repo-native check that can be run during future backend changes.
</success_criteria>

<output>
Create `.planning/phases/11-summary-quality-verification-and-documentation/11-01-SUMMARY.md` when done.
</output>
